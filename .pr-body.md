## 概要

RFC-0001 実装 intent の unit **merge-provenance**(C11 / FR-9、人間裁定 Q6=A + ADR-10)。

- `recordDelegatedMerge(evidence)` を新設 — 常任マージ承認(team.md ノルムが唯一の正本)条件下のマージ記録に、委任根拠参照 + 実測値(CI conclusion / converged digest)を残す **provenance 記録専用** API(マージの実行・判断はしない)
- 新監査イベント `DELEGATED_MERGE_RECORDED`(新カテゴリ merge-provenance — 既存 *_MERGED / MERGE_DISPATCH_* とは非転用)。event-registry / audit-format / pin テスト(93→94)を同一変更で同期
- 落ちる実証: 空・空白のみの evidence フィールドは loud 拒否(3+1 ケース)。新設 config なし
- CLI 配線は新規小ファイル `amadeus-merge-provenance.ts`(bolt.ts 共有制約の回避 + coverage 母集団膨張回避)

## 関連

- RFC-0001 / intent ミラー #3116
