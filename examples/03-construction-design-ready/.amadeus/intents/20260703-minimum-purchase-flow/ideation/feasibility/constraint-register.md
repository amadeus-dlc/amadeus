# Constraint Register：最小購入フロー

## 交渉不能な制約

| 制約 | 種別 | 根拠 |
|---|---|---|
| 在庫情報は在庫管理システムを参照元にする | 既存アーキテクチャ | 人間の指示で確定した統合制約である。steering の `external-systems.md`（EXT001）に対応する |
| 決済手段の拡張、会員ランク別価格、注文キャンセルは今回の対象にしない | 対象外 | Intent のモジュールファイルの `範囲` で確定した |
