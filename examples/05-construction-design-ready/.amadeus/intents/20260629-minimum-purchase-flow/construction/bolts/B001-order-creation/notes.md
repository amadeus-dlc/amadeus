# Construction ノート

## 実行方針

- B001 は、注文内容、購入者情報、販売可能在庫の参照結果を入力として注文を作成する Bolt として実装へ渡す。
- 実装時は、注文作成の責務を BC004 販売管理に閉じる。
- 決済、売上確定、在庫引当、出荷は対象外として扱う。
- この workspace では、アプリケーション実装コード、package 設定、テスト設定は確認できなかった。
  実装配置と実行コマンドは Implementation Execution で確定する。

## 対象タスク

| タスク | 状態 | 方針 | 証拠 |
|---|---|---|---|
| T001 | 未着手 | 注文作成入力を定義する。 | 未登録 |
| T002 | 未着手 | 注文作成前の不足条件を扱う。 | 未登録 |
| T003 | 未着手 | 注文内容と購入者情報を持つ注文を作成する。 | 未登録 |
| T004 | 未着手 | 対象外領域を注文作成から分離する。 | 未登録 |
| T005 | 未着手 | 作成された注文を顧客へ示す。 | 未登録 |

## 実装判断

- 注文作成の入力は、注文内容、購入者情報、販売可能在庫の参照結果を最小単位にする。
- 販売可能在庫の参照結果は、参照済みの結果として扱う。
  B001 では在庫管理へ問い合わせず、在庫引当も行わない。
- 注文番号、注文状態、永続化方式は未確認である。
  実装時に既存構造がない場合は、注文作成結果として顧客へ示せる最小情報に留める。

## 検証入口

- Amadeus 成果物検証: `bun run .agents/skills/amadeus-validator/validator/AmadeusValidator.ts . 20260629-minimum-purchase-flow`
- 実装テストの検証入口: 未確認

## 未確認事項

- 注文へ記録する購入者情報の具体項目は未確認である。
- 注文番号の採番方式は未確認である。
- 注文状態の有無と初期値は未確認である。
- 永続化方式は未確認である。
- 実装コードの配置とテストコマンドは未確認である。
