# Construction ノート

## 実行方針

- B001 は、B002 で確認された注文内容を入力として注文を作成する。
- 実装は、注文内容、購入者情報、販売可能在庫の参照結果、注文の最小データ構造から始める。
- 次に、確認済みの注文内容から注文を作成し、注文へ購入者情報と注文内容を記録する。
- 最後に、決済詳細、売上確定、在庫引当、出荷を行わないことを検証する。

## 対象タスク

| タスク | 状態 | 方針 | 証拠 |
|---|---|---|---|
| T001 | 未着手 | 注文作成の入力と出力の最小データ構造を定義する。 | 未登録 |
| T002 | 未着手 | 確認済みの注文内容から注文を作成する制御を実装する。 | 未登録 |
| T003 | 未着手 | 作成した注文に購入者情報と注文内容を記録する。 | 未登録 |
| T004 | 未着手 | 境界外責務が発生しないことを検証する。 | 未登録 |

## 実装判断

- この workspace に実装コードは存在しないため、B001 は新規の販売管理構成として実装する。
- B001 の Task は R004 と UC003 に対応させる。
- R001、R002、R003 は注文作成の入力前提として扱い、B001 の Task では直接実装しない。
- B002 の Construction Task はまだ存在しないため、tasks.md の依存は同一 Bolt 内の順序だけにする。

## 検証入口

- 後続の Implementation Execution では、B001 の Task ごとに観測できるテストまたは検証を追加する。
- 成果物構造の検証は次で実行する。

```sh
bun run .agents/skills/amadeus-validator/validator/AmadeusValidator.ts . 20260629-minimum-purchase-flow
```

## 未確認事項

- 注文へ記録する項目は未確認である。
- 注文番号、注文状態、永続化方式は未確認である。
- 注文作成に必要な項目が不足する場合の利用者向け表示は未確認である。
