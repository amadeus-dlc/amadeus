# Construction Notes

## 実行方針

B002 は B001 の guidance を基準に、template と eval の見出し契約を揃える。
source skill と昇格先成果物の両方を同じ内容にする。

## 対象タスク

- B002/T001。
- B002/T002。
- B002/T003。

## 実装メモ

- 標準 traceability template に `Construction からの追跡` を追加した。
- template eval の期待見出しに同じ見出しを追加した。
- 既存の完了済み Construction 例は `Construction からの追跡` を持つ。
- 未完了または Task Generation ready の example は完了時表を要求する対象外として扱う。

## 未確認事項

なし。
