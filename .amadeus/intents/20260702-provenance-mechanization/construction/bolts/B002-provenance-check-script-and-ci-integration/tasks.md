# Construction Tasks

- [x] T001: `provenance:check` の eval を先に追加し、RED を確認する
  - 作業:
    - `dev-scripts/evals/provenance-check/check.ts` を作成し、一時 workspace fixture で次を固定入力で検証する。
      - drift 3種の検出: md5 不一致、commit 不一致（記録された commit が git 履歴に存在しない）、参照先欠落（記録された repo 相対 path が記録 commit 時点で存在しない）がそれぞれ検出され、stdout へ1行1件で出力されること（BR004, BL005）。
      - スキーマ不適合（9項目の欠落、JSON 解釈不能）も失敗として扱われること（BR004）。
      - 対象なし: `provenance/` を持つ Intent が workspace に存在しない場合、失敗せず exit 0 であること（BR005, BL003, B002 完了条件）。
      - 入力エラー: 対象 workspace の path が不正な場合などは exit 1 であること（BR007）。
      - drift ありまたは入力エラーは exit 1、検出0件を含む正常時は exit 0 であること（BR007, BL006）。
      - `provenance/` ディレクトリを持たない既存 Intent が検査対象から除外されること（既存 Intent への遡及なし、BR005, INV002）。
      - workspace の絶対 path は照合対象にせず、記録だけを行うこと（BR009）。
    - このリポジトリ自体（実 workspace）への `provenance:check .` 実行結果も eval の検証項目に含める。実行時点で `provenance/` を持つ Intent が 0 件のため、検出 0 件・exit 0 が期待値になる。
    - `package.json` に `test:it:provenance-check`（`dev-scripts/evals/provenance-check/check.ts` を実行）を追加する。
    - 実装前に eval を実行し、失敗（RED）を確認して記録する。
  - 要求: R002, R003
  - ユースケース: UC002
  - 依存: B001/T002
  - 設計根拠: ../../U001-provenance-record-contract/functional-design/business-rules.md#業務ルール
  - 証拠: [test-results.md](test-results.md)

- [x] T002: `provenance:check` を最小実装し、GREEN を確認する。`npm run test:all` の chain に組み込む
  - 作業:
    - `dev-scripts/provenance-check.ts` を新設し、BL003（`.amadeus/intents/**` を走査して `provenance/` ディレクトリが存在する Intent だけを検査対象にする）、BL004（記録された `commit` と `path` の組から `git show <commit>:<path>` で記録時点の内容を取り出し、md5 を再計算して照合する）、BL005（drift 判定と1行1件の stdout 出力）、BL006（exit code 制御）を実装する。
    - 照合は記録時点の内容（`git show`）を対象にし、現在のファイルとは比較しない（BR003）。
    - `package.json` に `provenance:check`（実行入口）を追加する。
    - `package.json` の `test:it:all` chain の末尾（現行末尾の `test:it:list-unfinalized-intents` の後）に `test:it:provenance-generate` と `test:it:provenance-check` を追加する。
    - T001 の eval が pass する（GREEN）ことを確認する。
  - 要求: R002, R003
  - ユースケース: UC002
  - 依存: T001
  - 設計根拠: ../../U001-provenance-record-contract/functional-design/business-logic-model.md#業務ロジック
  - 証拠: [test-results.md](test-results.md)
