interface:
  display_name: "Amadeus Ideation Scope Definition"
  short_description: "Define Amadeus Intent scope and backlog"
  default_prompt: "Use $amadeus-ideation-scope-definition to define scope boundary and intent backlog for an Amadeus Intent."
policy:
  allow_implicit_invocation: false
