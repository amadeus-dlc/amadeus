{
  "name": "amadeus-claude-code-dev",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run dist && bun run promote:self",
    "dist": "bun scripts/package.ts",
    "promote:self": "bun scripts/promote-self.ts --apply",
    "source-only:check": "bun scripts/source-only-boundary.ts",
    "distribution:check": "bun scripts/mirror-distribution-check.ts && bun scripts/mirror-docs-contract.ts && bun scripts/scan-public-projections.ts",
    "distribution:recover": "bun scripts/distribution-transaction.ts recover",
    "distribution:benchmark": "bun scripts/mirror-distribution-benchmark.ts",
    "distribution:benchmark:aggregate": "bun scripts/mirror-distribution-benchmark-aggregate.ts",
    "test:all": "bun tests/run-tests.ts --all",
    "test:ci": "bun tests/run-tests.ts --ci",
    "coverage:ci": "bun tests/run-tests.ts --ci --coverage --coverage-dir coverage",
    "typecheck": "tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.tests.json",
    "lint": "bunx @biomejs/biome check tests/ packages/setup/ packages/framework/core/ scripts/ plugins/",
    "lint:check": "bunx @biomejs/biome check tests/ packages/setup/ packages/framework/core/ scripts/ plugins/",
    "no-silent-drop": "bun tests/no-silent-drop-gate.ts check",
    "check": "bun run typecheck && bun run lint && bun run distribution:check",
    "prepare": "lefthook install"
  },
  "description": "Dev-only tooling for the AIDLC framework — not part of the distributable. The shipped framework lives under dist/<harness>/ and is installed into user projects with `bunx @amadeus-dlc/setup install` (see README.md). All framework hooks and tools run via bun without requiring this package; bun-types here only powers contributor LSP coverage.",
  "author": "Amazon.com, Inc. or its affiliates",
  "license": "(MIT OR Apache-2.0)",
  "repository": {
    "type": "git",
    "url": "https://github.com/amadeus-dlc/amadeus"
  },
  "pi": {
    "extensions": [
      "./dist/pi/.pi/extensions/amadeus.ts",
      "./dist/pi/.pi/extensions/subagent.ts"
    ],
    "skills": [
      "./dist/pi/.pi/skills/amadeus"
    ]
  },
  "devDependencies": {
    "@anthropic-ai/claude-agent-sdk": "0.3.158",
    "@ast-grep/napi": "0.45.0",
    "@biomejs/biome": "2.5.5",
    "@opentelemetry/api": "1.9.1",
    "@opentelemetry/api-logs": "0.221.0",
    "@opentelemetry/context-async-hooks": "2.10.0",
    "bun-types": "^1.3.13",
    "fast-check": "^4.9.0",
    "lefthook": "^2.1.10",
    "typescript": "^6.0.3"
  }
}
