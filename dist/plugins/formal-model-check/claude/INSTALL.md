# Install: formal-model-check

Install through the host plugin marketplace using `.claude-plugin/plugin.json`.

Auto-compose runs from `hooks/hooks.json` on session start. To compose manually:

    bun .claude/tools/amadeus-plugin.ts compose
