\* Vacuity guard (cid:code-generation:vocabulary-collision-vacuity-guard).
\* EXPECTED TO FAIL: CloseUnreachable must be violated, proving the close
\* path is reachable within MaxReceipts and that NoCloseWithoutLandedSync
\* is therefore not vacuously true. Diagnostic only; never a CI job.
CONSTANTS
MaxReceipts = 3
SPECIFICATION Spec
INVARIANT CloseUnreachable
