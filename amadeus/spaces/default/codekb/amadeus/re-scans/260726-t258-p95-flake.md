# re-scan: 260726-t258-p95-flake

上流入力（consumes 全数）: Developer コードスキャン結果 `re2-dev-scan-result.md`（read-only scan、全文読了）。本 intent record には `scan-notes.md` が生成されず scratchpad に出力されたため、確定事実は本記録と `reverse-engineering-timestamp.md` に永続化する。

## スキャン諸元

| 項目 | 値 |
| --- | --- |
| intent | `260726-t258-p95-flake`（[Issue #1511](https://github.com/amadeus-dlc/amadeus/issues/1511) bug / P2 / S3-MAJOR） |
| Scope | `amadeus-bugfix`、Brownfield、単一 repo `amadeus` |
| Base commit | `f9a0fb86abaa2450d559cd04b4ee889d2271fd71`（前 intent `260726-mirror-state-split` の observed） |
| 祖先性 | `git merge-base --is-ancestor f9a0fb86a HEAD` **exit 0**、`git rev-list --count f9a0fb86a..HEAD` = **2**（cid:reverse-engineering:rescan-base-ancestry） |
| Observed commit | `09c669901385ad44e9a5b378b8d8903eebbc184c`（= 現 HEAD、`git rev-parse HEAD` 実測、author 日時 `2026-07-27 02:04 +0900`） |
| 区間規模 | `git diff --shortstat f9a0fb86a HEAD` = **32 files changed, 3709 insertions(+), 5 deletions(-)** |
| 面別内訳 | **32 ファイルすべて `amadeus/` record**（`git diff --name-only … | sed 's|/.*||' | sort -u` = `amadeus` のみ、`grep -vc '^amadeus/'` = **0**）。区間 2 コミット `2a52729fe`（前 intent RE+RA record）/ `09c669901`（前 intent CG+B&T record）はいずれも record snapshot |
| 患部の区間変更 | t258/t257・child helper・`amadeus-lib.ts`・`ci.yml` はいずれも区間内で**無変更**（source/test/CI 変更ゼロ） |
| 方式 | 差分リフレッシュ（フルスキャン不実施、cid:reverse-engineering:c1） |

## 欠陥の要旨（絶対 p95 予算の CI ジッタ偽赤）

`tests/integration/t258-lifecycle-transaction.test.ts`（467 行、`// @test-size medium` `:2`、timeout `120_000` `:466`）の性能契約テストが、**絶対 latency ceiling** を CI 共有ランナーのジッタで跨ぐ偽赤（フレーク）。

- **欠陥箇所（verbatim）**: `:461` `expect(result.archiveP95Ms).toBeLessThanOrEqual(500);` / `:462` `expect(result.recoveryP95Ms).toBeLessThanOrEqual(750);`。RSS `:463` `rssDifferenceP95MiB <= 96` は **noop 差分ベース**（`:444-447` の `archive.rssDeltaBytes - noop.rssDeltaBytes`）で該当外。
- **p95 集計**: `p95()` `:430-433` は nearest-rank `sorted[Math.ceil(len*0.95)-1]`（len=100 → `sorted[94]`）。上位 5 サンプルの予算超過は許容、**6/100 超過で初めて fail**。
- **被測定**: child helper `tests/helpers/lifecycle-transaction-benchmark-child.ts`（size=10000）が `spawnSync` 1 プロセス起動で **10,000 行 registry/audit の実 FS transaction**（`withIntentLifecyclePreflight` / `runIntentLifecycleTransactionLocked`、`packages/framework/core/tools/amadeus-lib.ts`）を測る。elapsed は transaction 区間のみ（spawn は含まない）。CPU よりも **FS I/O 律速**。
- **機序**: `bun run test:ci -- -P 4`（`.github/workflows/ci.yml:162` name / `:163` run）の**並列度 4 integration tier**（専用 perf ジョブ・リトライ・負荷分離なし）で 10,000 行 I/O が IO/CPU 競合しスパイク → 絶対 ceiling を 6/100 超が跨ぐと偽赤（cid:code-generation:fanout-load-settle-before-integration / cid:code-generation:rerun-red-reattribution クラス）。Issue #1511 実測: main push run 30193617264 で t258 + plugin discovery gate の 2 件 failure、PR #1504 diff は t258 と非交差・同 PR の pull_request run はフル green（負荷依存の相対タイミング契約クラス）。類例 30185062036（#1493）。

## 予算の出所（500/750 はどこから来たか）

- **導入コミット** = `2e157d7fe`「archived intent statusと誤resume防止を導入 (#1424)」。500/750/96 は t258 追加と同一コミットで導入（`git log -S "toBeLessThanOrEqual(500)"` = `2e157d7fe` のみ）。導入時から `:461-463` に rationale コメントなしの裸マジックナンバー。
- **決定の出所** = intent `260723-archived-status-guard` の nfr-requirements。Options「500ms/750ms, 1s/2s, N/A, Other」から **A 案（500/750）をユーザーが選択**した round-number 予算で、CI ジッタ / noise floor から導出されたものでは**ない**。
- **実測との乖離** = 同 intent code-summary の「最終 full CI 実測: archive p95 **41.177ms**（上限 500）、FFF recovery p95 **29.314ms**（上限 750）、noop 差分 RSS p95 **45.984MiB**（上限 96）」。予算は実測 p95 の約 12〜25 倍ヘッドルームだが、**絶対 ceiling** ゆえ負荷スパイク時に個別サンプルが容易に 500ms を跨ぎ、6/100 超で fail する。

## Architect 段の独立再検証（observed `09c669901`）

上流 scan の核心 file:line を observed で spot-check 直読照合した（cid:reverse-engineering:cite-shift-vs-nonshift-separation）。

| 照合対象 | 結果 |
| --- | --- |
| assert: `t258:461` `archiveP95Ms<=500` / `:462` `recoveryP95Ms<=750` / `:463` `rssDifferenceP95MiB<=96` | 一致 |
| p95: `t258:430-433`（nearest-rank `Math.ceil(sorted.length*0.95)-1`） | 一致 |
| noop baseline: `t258:444-447`（`archive.rssDeltaBytes - noop.rssDeltaBytes`） | 一致 |
| size/timeout: `t258:2`（`// @test-size medium`）/ `:466`（`120_000`） | 一致 |
| same-root: `t257:240-241`（`strictReadP95Ms<=100` / `migrationP95Ms<=250`） | 一致 |
| 安全形: `t259:209/211`（`p95(archived)-p95(allowed)<=100ms` / RSS `<=16MiB` = baseline 相対差分） | 一致 |
| same-root 全数 grep `P95Ms).toBeLessThanOrEqual` | t257（`:240-241`）+ t258（`:461-462`）のみ（t259 は差分形で非マッチ） |
| CI: `ci.yml:162` name `- name: Tests - smoke + unit + integration` / `:163` run `bun run test:ci -- -P 4` / `package.json:19` `test:ci` | 一致（scan の `:162` は name 行を指す。run 行は `:163` と精密化） |
| 予算 provenance: `git log -S "toBeLessThanOrEqual(500)"` = `2e157d7fe`（#1424） | 一致 |

**訂正: 0 件。** CI 行番号は scan の `:162`（name 行）に対し run 行が `:163` であることを精密化したのみ（訂正ではない）。

## 同型先例（修正様式の参照点）

| 先例 | 述語 SHAPE | 教訓 |
| --- | --- | --- |
| `tests/lib/plugin-discovery-overhead-gate.ts`（#1525、現 tree 実在） | `exceedsDiscoveryOverhead` = `additionalMs/baseline > 0.2`（相対比）**AND** `additionalMs > 10ms`（絶対 noise floor、worst jitter 実測 4.36ms の ~2.3x）+ fail-closed（`!(baseline>0) || !Number.isFinite(treatment)` → true） | 判定述語を計測ループから分離し in-process 駆動可能に。`median()` は長さ非依存 |
| `scripts/mirror-distribution-benchmark-aggregate.ts`（#1507、94 行） | 権威判定 `median(p95) > budget`（median 基準）+ `absoluteSpread > p95Budget*0.05`（絶対 spread noise floor）の 3 条件 AND | 単一スパイク replica 単独では落ちない |
| `tests/integration/t259-guard-integration.test.ts:209/211`（同ドメイン安全形） | `p95(archived)-p95(allowed) <= 100ms` / RSS `<= 16MiB`（baseline 相対差分） | #1511 クラス非該当の参照実装 |

**共通原則**: 絶対 ceiling 単独をやめ、(median/baseline 基準) AND (絶対 noise floor) の複合述語にし、判定述語を計測ループから分離して fail-closed に。t258 は RSS 用に **noop baseline を既に測っており**（`:444`）、archive/recovery latency も noop 相対へ転用できる素材が既存（事実。方式は設計段で裁定）。

## 合成上の主要な確定事項（修正設計はしない）

1. **欠陥の核** = `t258:461-462` の絶対 latency ceiling 500/750ms。RSS `:463` は noop 相対で該当外。
2. **予算は #1424 のユーザー選択 round number**、CI 実測 p95 = 41.177 / 29.314ms（record 実在）で noise floor 由来ではない裸マジックナンバー。
3. **機序** = 10,000 行 I/O transaction を `-P 4` 並列 integration tier（単一 CI ジョブ、負荷分離なし）で絶対 ceiling 判定 → 6/100 超スパイクで偽赤。
4. **同型先例 2 件が「median/baseline AND noise floor + 述語分離 + fail-closed」を確立済み**。noop baseline は t258 に既存。
5. **same-root**: `t257:240-241` が同根（#1511 未報告） — 修正時は同一 PR 修正 or Issue 化で同根全数を閉じる（cid:code-generation:same-root-inventory）。`t259` は既に安全形。
6. **修正方式は未裁定** — noop 相対 + noise floor 複合述語 vs 予算緩和、判定述語の計測ループ分離、t257 同根の同時修正、専用 perf ジョブ分離の是非は requirements-analysis 以降で確定する。予算単純緩和は「絶対 ceiling ゆえ負荷次第で跨ぐ」構造を温存し先例が否定した方向。

## センサー不適用と代替検証

RE ステージが宣言する 3 センサー（`required-sections` / `upstream-coverage` / `answer-evidence`）は、codekb 出力パス `amadeus/spaces/default/codekb/amadeus/**` が各 manifest の filter（`**/{amadeus-docs,intents}/**` および `**/*-questions.md`）に**構造的に不適合**であり発火不能（cid:reverse-engineering:re-sensors-codekb-filter-mismatch、cid:reverse-engineering:c3-codekb-sensor）。**センサー成功として扱わない。** 代替として以下を実施。

**(a) H2 見出し数の機械確認（`grep -c '^## '`、H2 ≥ 2 を要件とする）** — 更新した body 成果物 + 本ファイルすべてで H2 ≥ 2 を確認。

**(b) 上流入力への実参照の確認** — 更新した全成果物および本ファイルの本文に上流入力 `re2-dev-scan-result` への参照が実在することを `grep -c 're2-dev-scan-result'` で機械確認（全件 1 以上、本文の依拠箇所からの参照）。

**(c) 旧「現在」マーカーの降格確認** — `grep -rn '、現在、\|（現在:' amadeus/spaces/default/codekb/amadeus/*.md` の残存ヒットが本 intent `260726-t258-p95-flake` の節のみであることを機械確認。前 intent `260726-mirror-state-split` の H2 は「履歴」へ降格済み（cid:reverse-engineering:c3-relabel）。

## Delivery boundary

本 scan の成果物は codekb 9 成果物の差分更新と本 per-intent 記録のみ。患部テスト（t258/t257）・child helper・CI 設定・coverage allowlist・GitHub Issue の操作・intent record / state / audit・生成配布物への書込は一切行っていない。修正方式は後続の requirements-analysis 以降で裁定する。
