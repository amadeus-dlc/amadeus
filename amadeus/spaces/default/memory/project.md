# Project-Level Rules

> 2026-08-12 ノルム蒸留(ユーザー直接裁定): 具象ルール・票証跡・チームモード専用ノルムを削除し原理原則へ縮約。旧文面は git 履歴(この整理前のコミット)に保存。
>
> このプロジェクト固有の技術・構造・運用上の追加規則。org.md、team.md に矛盾しない規則を加算する。ランタイム、リポジトリ構造、CI、配布経路など、このプロジェクトを離れると成立しない規則をここに置く。チーム共通の判断・レビュー・エスカレーション規則は team.md、一時的な intent 制約は intent record に置く。

## Way of Working

実装時は `packages/framework/core/`(ハーネス中立の正本)または `packages/framework/harness/<name>/`(ハーネス別表層)を編集元とし、`dist/` とセルフインストールツリーは未追跡のローカル生成物として `bun run build` で再生成する。生成物を独立した正本として編集しない。

- 大規模な initiative を規模だけを理由に複数 intent へ分割しない。1 intent が監査・状態機械・trace の anchor であり、分断すると audit trail が分断される。並行化は Unit 設計と Construction Bolt の並行実装で実現する <!-- cid:intent-capture:c4-2 -->
- Unit・Bolt の定義と粒度は正準(`stage-protocol.md` Glossary と `delivery-planning.md`)に従い、ノルム側で再定義しない。ノルムが定めるのは2点: (a) Unit は独立に実装可能であることを Delivery Planning 前に検証し、片側だけでは価値を出荷できない境界は単一 Unit へ統合する (b) PR 粒度は Bolt ごとを既定とし、複数 Unit・他 intent の工程記録・無関係リファクタを単一 PR に束ねない(自 intent の record checkpoint の同梱は可 — team.md Way of Working / E-260813-RECORD-BUNDLING-NORM に整合)。Issue 起点は **1 Issue = 1 Unit** を原則とし、依存・共有ファイル競合のないものは並行化する。Unit 分割では source と test file の ownership を同じ境界へ揃える <!-- cid:units-generation:c1 -->
- ソロ運用でも Bolt 実装は最初から git worktree 分離で行い、本線ツリーのブランチ切替で実装しない。source-only 境界下の新規 worktree は自己インストール面を欠くため、依存インストールと `bun run build` を移設の定型手順に含める <!-- cid:code-generation:solo-bolt-worktree-required -->
- サブエージェントに engine/state の変更操作(`amadeus-orchestrate.ts` の next/report/park、`amadeus-state.ts`、`amadeus-log.ts`、`amadeus-bolt.ts`)をさせない。状態遷移・ゲート提示・レビュー・学習リチュアルは conductor のみが行う。prompt 明示だけに頼らず、調査・レビューは書込不可のサブエージェント種別に限定し、drafting では 1 エージェント = 1 成果物パスに固定する <!-- cid:practices-discovery:c2-engine-mutation-ban -->
- 常任グラント(standing grant)が覆うのはステージゲートの承認のみで、内容裁定の代答には使えない <!-- cid:approval-handoff:c2-grant-gates-only -->
- semi/full の Intent autonomy が有効な間、§13 学習選定やステージ内の判断質問は人間へ直接提示せず `amadeus-bolt decide-question` の梯子で裁定し、fail-closed の結果のみ人間へ回す。常任グラントとは別機構であり適用境界を混同しない。§13 学習選定については、本則が team.md の `cid:requirements-analysis:learnings-election`(「§13 学習選定は選挙にかける」)を狭める形で優先する — autonomy none では従来どおり選挙が正規経路であり、本則はそれを否定しない <!-- cid:scope-definition:c1-semi-ladder-routing -->
- AskUserQuestion が dismiss された場合は沈黙を承認とみなさず、推奨値も選択扱いにしない。適用可能な最も具体的な既存規則で判定し、根拠を成果物へ明記する <!-- cid:practices-discovery:c2-dismiss-not-approval -->
- units-generation を SKIP する degrade スコープでも、code-generation 成果物は `construction/<slug>/code-generation/` の unit ディレクトリ様式に置く。複数 unit の並行実装では、unit ディレクトリ作成と directive 捕捉を conductor が unit ごとに直列で所有する(全 unit covered 後の再取得は fail-closed) <!-- cid:code-generation:c1-degrade-batch-directive-capture -->
- Issue と承認済み成果物にある決定を再質問しない。質問は矛盾か、実装を阻む要件欠落に限る。UX・互換性など既存実装の流儀で決まる事項は既存パターンを読んで合わせる <!-- cid:requirements-analysis:c5 -->
- ワークフロー完了の確定は、stage body の全 Steps 実行・mirror boundary の receipt 完結・Bolt 配送の確認を経てから行う。state ポインタの前進を実体完了と同一視しない。誤って complete へ進めた場合は履歴 rewrite せず前進 revert(state と ledger のみ、audit 無改変)で回復する <!-- cid:build-and-test:bt-workflow-completion-substance-gate -->

## Walking Skeleton

スコープ別の walking-skeleton 既定は org.md に従う。greenfield 要素(新パッケージ・新配布経路など)を含む intent では、最初の Construction Bolt を小さな end-to-end スライスとして扱い、以後の拡張前に人間がゲートで確認する。

## Testing Posture

テストは TypeScript で `tests/` 配下に追加し、Bun ベースの既存ランナーで検証する。PR/CI の基準は `bun run typecheck`、`bun run lint`、隔離2回ビルドの再現性検査、`bun run source-only:check`、グラフ不変量検査、`bash tests/run-tests.sh --ci` に加え、Project Coverage Gate の固定絶対下限と merge-base 相対許容低下幅の両条件、Patch Coverage Gate、plugin-conformance-e2e を含む現行のブロッキング集合全体を満たすこととする。Project Coverage Gate の絶対条件と相対条件は AND 条件であり、片方だけの通過を coverage 達成と扱わない。ユーザー可視の契約(CLI 契約・Release Asset 配布・セルフインストール互換など)は該当領域を触る変更で必ずカバーする。

既存テストが赤い場合は変更前のベースラインを確認する。自分の変更による失敗は必ず直し、既存の無関係な失敗は安全かつ低コストなら修正し、それ以外は Issue に記録してスコープを膨張させない。

- filesystem / process を使う medium test は unit allowlist を増やさず integration suite に置く <!-- cid:code-generation:c2-doctor-seam -->
- property-based test(fast-check)は unit 層の Developer Testing として既存スイートに常駐させ、別枠の QA スイートを新設しない。PR CI は固定 seed・低 numRuns で即再現可能に保ち、深掘りは別ジョブへ分離して失敗 seed をログ化する。新設・変更する永続化境界(write⇔read、発行⇔消費)には round-trip プロパティを標準観点として付ける <!-- cid:build-and-test:pbt-developer-testing-posture -->
- PBT のオラクルで、検証したい不変量そのものを被検実装から独立に再実装しない。両者が同じ箇所で正しく振る舞うと欠陥が観測面に出ず相殺され、緑のまま欠陥が生存する <!-- cid:build-and-test:pbt-oracle-cancellation -->
- 二層検証(team.md § Testing Posture)の形式検証面: 有限探索(TLA+/TLC)で「検出されなかった」と主張できるのは、宣言済み有限 domain の固定点まで完走した completion marker と state 統計が揃う場合だけで、部分探索・timeout・統計欠損は fail-closed に扱う <!-- cid:application-design:finite-exploration-not-detected-proof -->
- `TEST_TIME_FACTOR` はテスト用 timeout の基準値への乗算を中心契約とし、timeout の成立と検証に対応する sleep・poll・settle にも同じ係数を適用する。乗算を直接書かず `tests/lib/test-time-factor.ts` の `scaleTestTime` を経由する。性能基準や本番 CLI の timeout 契約は対象外 <!-- cid:requirements-analysis:test-time-factor-c1 -->
- 長い本番タイムアウトを持つ性能要件は、実時間の負荷試験ではなく、同じ制御経路を通る短縮可能なタイミングシームとカウンタ検証で構成する <!-- cid:build-and-test:bt-timeout-verification-shape -->
- Test Strategy の水準が高くても、performance / security の検査は承認済み NFR と実在境界へ trace できる範囲だけ生成し、生成しなかった検査は根拠付きで明記する。合否を決める数値目標が要件に宣言されていないテスト種別は、体裁のために実体を作らない — 指示書に「適用可能な NFR が存在しないという判定」であること、根拠、将来この判定を覆す条件を明記する。目標なきベンチマークは検証劇場、無言の省略は黙示の欠落 <!-- cid:build-and-test:c2-no-test-theatre-for-absent-nfr -->
- テストファイル・テストヘルパを新規追加した変更では、範囲を絞った実行の緑で完了としない。conductor がフルスイートを1回通す(横断ゲートは絞り込み実行の射程外) <!-- cid:code-generation:c3-conductor-runs-full-suite -->
- verdict では検証した面と未検証の面を書き分ける(起動の成立を配送の成立へ昇格させない)。未検証面が受け入れ基準の外にあると requirements/RAID の実文照合で確認できる場合は無条件 READY としてよく、未検証面は申し送り節に列挙する <!-- cid:build-and-test:verdict-names-unverified-facets -->
- 要件・RAID が「実装時に実測確認」と規定した項目を後続 intent へ先送りしない。受け入れ基準が名指す経路そのもので確認し、内部関数の単体実行や部分経路で代替しない <!-- cid:build-and-test:no-silent-scope-narrowing -->

- 監査互換性を保つ SENSOR_PASSED + tool-unavailable 行を維持しつつ、blocking guard では usable evidence でない診断として fail-closed に扱う。 (learned 2026-08-18) <!-- cid:code-generation:c2-issue3029 -->
- Comprehensive strategyでも、性能・security の NFR が宣言されていない変更では、N/A 判定と根拠を instruction に記録し、目標なきテストを発明しない。 (learned 2026-08-18) <!-- cid:build-and-test:c1-build3029 -->
- ローカル targeted validation と baseline 比較を済ませた後は、full CI の反復ではなく push-first でリモート CI を正本にする。 (learned 2026-08-18) <!-- cid:build-and-test:c2-build3029 -->
## Deployment

デプロイ基盤は持たず、リリースは npm パッケージ配布、GitHub Release Asset、タグ / PR 履歴で管理する。GitHub Actions は push と pull_request で typecheck、lint、隔離2回ビルドの再現性検査、source-only 境界検査、グラフ不変量検査、smoke + unit + integration tests を実行する。

リリースは `release.yml` の `workflow_dispatch` 一本で行う。lander がバージョン同期を bot PR と merge queue で着地させ、squash commit に `vX.Y.Z` タグを打ち、GitHub Release ノートと npm publish を実行する。手書きの `CHANGELOG.md` は持たず、機能 PR や amadeus ワークフローからバージョンを上げない。

- バージョン管理下の append-only 生成物の誤りは、履歴 rewrite・force push・branch protection 緩和をせず、人間承認付き通常 PR の `git revert` で回復する。collector / schema の欠陥なら修正と revert を同一 PR にし、conflict 時は停止する <!-- cid:deployment-pipeline:c3 -->
- Deployment Execution の結果は N/A(反証可能な不存在・非適用根拠あり)、NOT EXECUTED(理由併記)、PENDING(閉包条件併記)、PASS(実行証跡に基づく検証成功)を相互代用しない <!-- cid:deployment-execution:c3 -->

## Code Style

TypeScript / ESM と Bun 直接実行を前提に、既存の `amadeus-` プレフィックス、`packages/framework/core/` のハーネス中立層と `packages/framework/harness/<name>/` のハーネス別表層という境界を守る。フォーマッタは無効、lint は Biome、型検査は `tsc --noEmit` で行い、ツール・フックに実行ビットを要求しない。

- DECIDED: ドメインモデリングは `amadeus/spaces/default/knowledge/amadeus-shared/software-design/functional-domain-modeling-ts/`(class-free、type + コンパニオンオブジェクト、ブランド型 + スマートコンストラクタ、判別ユニオン Result)を採用する。普遍原則(tell-don't-ask、parse-don't-validate、first-class-collection 等)はこのスタイルの上で適用する
- ドメイン型は type にインスタンスメソッドを宣言し(実装は内部ファクトリ + クロージャの frozen リテラル)、コンパニオン namespace は static 相当(parse / build / コレクション演算)のみを持つ。貧血型も全面 static 寄せもどちらも誤り <!-- cid:functional-design:c11 -->
- 既存 API の戻り値が実検出値と fallback を同じ表現へ潰す場合、公開互換性を保ちつつ内部 resolution に検出元 provenance を保持し、両者を区別してから後続判定へ渡す <!-- cid:application-design:c1 -->
- md 表への機械可読メタデータは行内 HTML コメントでなく fenced YAML ブロックで持つ(行末コメントのセル境界は処理系依存の無音破損クラス) <!-- cid:functional-design:c4-yaml-block-over-inline-comment -->
- ユーザー向けの質問と成果物では、正式定義されていない略称や専門用語を使わない <!-- cid:requirements-analysis:c3-260802-plugin-optin-parity -->

## Tech Stack

- ランタイム / パッケージマネージャ: Bun(TypeScript、ESM)。hooks / tools はすべて bun で直接実行する
- 言語: TypeScript(`tsc --noEmit` で型検査)/ リンター: Biome(フォーマッタ無効)
- テスト: bun test ベースの自作ランナー `tests/run-tests.sh`(smoke / unit / integration / e2e の4層)
- 主要開発依存: agent SDK、Biome、TypeScript、fast-check。TUI E2E は Bun から tmux バックエンドを駆動する
- 構成: `packages/framework/core/`、`packages/framework/harness/<name>/`、`scripts/package.ts`(ビルド)、`dist/<harness>/`(未追跡)、`docs/`

## Decided

- DECIDED: 新しい `/amadeus --*` ユーティリティハンドラを実装する前に `docs/reference/11-contributing.md` の「Adding a Utility Handler」チェックリストに従う
- DECIDED: バージョンバンプは `release.yml`(workflow_dispatch → `scripts/release-land.ts`)だけが行う。`scripts/release-version-sync.ts` が全バージョン面を機械的に同期し、着地は merge queue 上の bot PR に限定する。機能 PR や amadeus ワークフローが個別ファイルのバージョンを上げることはない
- DECIDED: 成果物が複数の配送経路(packager 投影 / runtime compose / self-install 等)を通って届く修正では、受け入れ条件をソース断面の述語で書かず、各配送先の実ツリーに対する述語で書く。ソース断面だけの green は、変換器を持たない配送路の退行を構造的に隠す <!-- cid:requirements-analysis:c2-acceptance-at-delivery-tree -->
- DECIDED: `packages/framework/core/` を変更したときの build と再現性検査は、manifest が発見する全ハーネスを対象とする。固定数や一部列挙で止めず、packager の検出集合を正とする <!-- cid:build-and-test:bt-dist-regen-seven-harnesses -->

## Scope Overrides

- DECIDED: Amadeus 自体の新機能や仕様変更を扱う intent には `self-feature` を明示して開始する。既存 intent の再開では state に記録されたスコープを使う <!-- cid:scope-definition:default-scope-amadeus -->
- DECIDED: 既存設計・方針・ハーネス間契約との不一致を限定的に是正する intent は `self-fix` を既定とする(不具合、parity、生成物・文書の drift、既存方針の訂正)。Issue 起点は種別を問わずクロスレビュー2名成立を前提とし、intent-first のミラー Issue は record PR の独立2名レビューを前提とする。新機能・新仕様が必要ならユーザーの明示指示を得て `self-feature` へ切り替える <!-- cid:scope-definition:bugfix-scope-for-bug-intents -->
- DECIDED: 外部から観測できる振る舞いを変えず内部構造を改善する intent には `self-refactor` を明示して開始する <!-- cid:scope-definition:refactor-scope-for-refactor-intents -->
- DECIDED: 正規スコープは `self-feature` / `self-fix` / `self-refactor` / `self-document` とし、旧 `amadeus` / `amadeus-bugfix` / `amadeus-feature` / `amadeus-fix` / `amadeus-refactor` は廃止した。互換エイリアスは提供せず、履歴 record 内の記録値のみ監査履歴として保持する <!-- cid:scope-definition:legacy-amadeus-resume-only -->

## Forbidden

- NEVER 手書きの `CHANGELOG.md` を復活させない。リリースノートは release.yml の GitHub Release 自動生成ノートが唯一のソース
- NEVER 既存テストの赤を「自分と無関係」を理由に無視して続行したり、赤いスイートをグリーン・完了として報告したりしない
- NEVER インストーラの挙動を変更するとき、正本・配布物・セルフインストールツリーをコミット間で不整合にしない
- NEVER 隔離2回ビルドの再現性検査、source-only 境界検査、グラフ不変量検査を、ローカルだけの手動チェックリストで代替しない
- NEVER 利用者側の Bun-only 前提を変更する理由を文書化せず、配布フレームワークへ runtime dependency を追加しない
- NEVER `packages/framework/core/` / `packages/framework/harness/` の維持または移動を、ADR / 設計記録なしに暗黙決定しない
- NEVER `dist/` の追跡境界・配置・配布経路の変更を internal refactor として扱わない。README、docs、tests、self-promotion、CI、installer、release asset への影響を棚卸しする
- NEVER 生成された `dist/` やセルフインストールのコピーを独立した正本として編集しない。これらは破棄可能なローカルビルド出力であり、レビュー・コミット対象ではない
- NEVER ローカル filesystem 上の不在を根拠として、実在しないパッケージ構成を捏造しない
- NEVER walking-skeleton stance が有効なとき、standing grant に walking-skeleton gate を認可させない
- NEVER 想定内の grant 失効・取消・scope 不一致 fallback を、fatal error 経路へ流さない
- NEVER `intent-mirror.github.issue.mode` に boolean 値を受け付けない
- NEVER `intent-mirror.github.issue.mode = auto` の consent を、PR マージ、リリース、publish、デプロイ、無関係な外部操作へ拡張しない
- NEVER Amadeus の所有 provenance が不在または不整合な Issue を自動で編集・クローズしない
- NEVER GitHub mirror の失敗を、同期状態の無音喪失や AI-DLC ワークフローの恒久停止の理由にしない

## Mandated

- ALWAYS `packages/framework/core/` または `packages/framework/harness/<name>/` を正本として編集し、`bun run build` で未追跡の `dist/` とセルフインストール面を再生成する。全ハーネスに影響する正本変更後は build を実行し、追跡ファイルが不変であることを確認する
- ALWAYS framework source、全ハーネス配布、self-install 面、tests、対訳ドキュメントを同じ変更で更新する
- ALWAYS `dist/` またはセルフインストールツリーの path を変える案では、再現性検査、source-only 境界検査、グラフ不変量検査、Release Asset と installer の互換性を同じ成果物に書く。Release Asset はクリーン checkout の release workflow だけが生成し、ローカル生成物との byte parity を追跡ファイルへ要求しない
- ALWAYS 新設パッケージ(`packages/*`)は lint(Biome)と型検査(`tsc --noEmit`)の配線をパッケージ追加と同一 PR で加える
- ALWAYS harness 専用ツールを `packages/framework/core/tools/` に置かない(全ハーネス manifest の coreDirs が tools を投影するため構造的に全 dist へ漏出する)。harness 専用は `packages/framework/harness/<name>/tools/` + harnessFiles 投影に置く <!-- cid:code-generation:harness-tools-placement -->
- ALWAYS active scope が `self-feature` なら、既存コードを変更する場合も最初の Construction Bolt に walking-skeleton gate を維持する
- ALWAYS 認可に関わる変更を directive contract、state transition、audit invariant、race、harness drift のテストで検証する
- ALWAYS 変更が触る path が要求する strict typecheck、Biome lint、関連テスト、coverage gate、complexity 検査、配布 drift 検査で検証する
- ALWAYS Intent record を正本とし、GitHub mirror は record → Issue の一方向で同期する。retry は GitHub の部分成功とローカル state 書込失敗を跨いで冪等にする
- ALWAYS mirror Issue を自動クローズする前に Amadeus の所有 provenance とワークフローの着地を検証する
- ALWAYS GitHub の可用性・認証・権限・rate-limit・コマンド失敗の後もワークフローを継続し、未同期の警告と retry 状態を可視に記録する
- ALWAYS 明示的な `intent-mirror.github.issue.mode = auto` は、active Intent に限定した mirror の create / sync / 所有 provenance 確認済み close と、ラベル同期への standing consent として扱う。`intent-initialized` と `intent-capture-approved` 境界で関連 Issue に `in-progress` を付与し、`workflow-completed` 境界で除去する。ラベル同期は fail-open とし、失敗を警告として記録して継続する
- ALWAYS telemetry の export 境界(送出点)でも redaction filter を通す。write-time のみの redaction に留めない <!-- cid:practices-discovery:export-boundary-redaction -->
- ALWAYS markdown artifact は日本語で書く。ただし path、CLI、コード識別子、tool が要求する heading は正確性を優先して保持する
- ALWAYS gh CLI は optional dependency として扱う。利用前に runnable / auth readiness を検査し、不在・未認証・API 障害は当該 invocation を loud fail する。credential は gh の store へ委譲し token を保持・出力しない。create / close の人間承認境界は維持する <!-- cid:practices-discovery:gh-scripts-boundary -->

## CI/CD

- PR を merge-ready と判断する前に、未解決の review thread と未確認のトップレベルコメント / review summary をゼロにする。指摘は対応するか根拠を示して却下したうえで resolve し、resolve 状態を持たないコメントには PR 作者が対応内容または却下理由を返信して確認済みにする。確認文だけで指摘対応を代替しない。最後に最新 HEAD で必須 CI が green であることを実測する <!-- cid:ci-pipeline:review-feedback-closure-before-merge -->
- `main` の Ruleset は Merge Queue を必須とする。PR-level の必須 CI と review feedback closure が green になった PR は、`main` の前進だけを理由に手動 rebase を繰り返さず queue へ投入する。merge-ready の正本は、最新 `main` と queue 内の先行 PR を合成した merge group に対する必須 CI の green とする。自動 PR も Ruleset を bypass せず queue を通す <!-- cid:ci-pipeline:strict-up-to-date-before-merge -->
- CI ゲートを「blocking として実行する」要件は、赤がマージを止めることを要求する。必須 status check が集約ジョブ1件の構成では、集約の `needs` に載らないジョブは advisory に留まる。「いつ走るか」(needs / if を持たない独立性)と「赤が止めるか」(集約の needs への所属)は独立した2面である <!-- cid:code-generation:c1-2814-aggregate-needs-is-blocking -->
- docs を検証対象に含むテストを持つこのリポジトリでは、docs-only の paths-ignore が「doc 変更 → ガード素通り → latent 赤」を構造的に作る。doc-consuming テストが読む doc は paths-ignore から除外するか、ガードを件数フリー契約にする <!-- cid:build-and-test:ci-paths-ignore-doc-guard-blindspot -->
- CI の `TEST_TIME_FACTOR` 既定値は `2` とし、`3` はより低速な実行環境の override として残す <!-- cid:requirements-analysis:test-time-factor-c2 -->
- Snapshot 等の非 blocking job は PR blocking 集約の外に置いてよいが、`main` 上の失敗は赤く可視化する。非 blocking の目的と loud-fail 契約を明記し、一般の必須 CI gate を除外する根拠にしない <!-- cid:ci-pipeline:c3 -->
- 既存 workflow に実装済みなら新規 workflow を二重生成せず、既存 workflow を唯一の正本として文書化・検証する <!-- cid:ci-pipeline:c2 -->
- no-silent-drop ゲートの台帳は追記型 ULID イベント台帳(`tests/no-silent-drop/events/<ulid>.json` — grant / revoke / snapshot)を正本とし、実効集合は順序非依存の集合演算で導く。feature PR は event 追加のみとし、削除・snapshot は maintenance CI 専用。新規 grant は reason 非空と issue 参照1件以上を fail-closed で要求する <!-- cid:code-generation:c1-260803-state-integrity -->

## Corrections

このプロジェクトの構造に根ざす恒久的な是正知識のみを置く。個別 intent の一回性の罠・ツール世代依存の回避策は記録しない(git 履歴と intent record が一次記録)。

- 検査・ゲートの閾値は、対象コーパスへ実述語を適用した**観測レンジの内側**に置く。レンジ外の閾値は全件赤か全件緑になり「どれが外れ値か」を返さない。受け入れテストは**両側**(観測最小値 < 閾値 < 観測最大値)を固定する。水準ごとに位置づけが異なるなら共通規則へ丸めず個別に固定する。ベンチマークの baseline 相対項は、baseline 系列が対象系列と負荷を共有することを実測してから採用する — 空ウィンドウ baseline は絶対判定へ無音退化する <!-- cid:code-generation:c1-threshold-inside-observed-range -->
- 同一 worktree での coverage 計測は branch ごとに単独所有者を決めて直列化する。runner が起動時に coverage ルートを削除するため、並行実行は相互破壊しどちらの verdict も信頼不能になる。フルスイートを合否判定に使う実行中は重いコマンドを並行実行しない — conductor 自身が負荷源になりうる <!-- cid:code-generation:c1-coverage-single-owner -->
- shrink-only ratchet ガードの初期 census は、実装時点の base ではなく**マージ先の最終 base** で採る。増加を admit できない単調ガードでは、base 前進が新規違反を持ち込んだ時点で構造的に赤が確定する。coverage allowlist の行ピンも、行シフトを跨ぐ変更では全エントリを機械 remap したうえで reason と現行行内容の一致を直読照合し、waiver レンジの span 膨張による fail-open も検査する <!-- cid:code-generation:c5-ratchet-census-at-final-base -->
- 帰属の切り分け(自変更由来か既存・環境起因か)は、未改変ベースで**同一条件**を再現し失敗テスト名の集合差で判定する。ベースコミットを自分自身と比較する形は自己参照であり証拠にならない。同一条件には gitignored な外部入力(active-intent cursor、env 等)も含める。生成物依存が疑われる場合は3段で扱う: (1) 許可済み入力のみを植えた ablation を先に試す (2) 存在依存と内容依存を区別する (3) 真に内容依存が残る場合のみベース側で**再生成**して比較する — byte-copy は自変更由来の欠陥を masking するため禁止 <!-- cid:build-and-test:c1-ablation-before-artifact-repro -->
- 消費者の棚卸しは毎回 grep 出力からの転記で作り直し、既存表から複製しない。検索キーは複数軸で持つ: 変数名と展開後リテラル、同一構造への複数アクセス形式、path のセグメント結合形、対訳ドキュメントの実語彙(逐語訳とは限らない)。自然言語 prose の sweep は既定を大小文字非区別とする。全数棚卸しの件数を成果物へ書くときは、再実行して同一結果を得られる検索述語(パターン・対象集合・除外条件)を結果と同じ場所に記録する <!-- cid:application-design:dual-key-consumer-inventory -->
- 実測値(件数・SHA・時刻・pass/fail 数)には取得コマンド・測定対象の ref または tree・列挙元・再計算方法を併記する。再導出できない実測値は実測ではなく主張である。派生値(平均・按分)は算出式を併記し、未実測の推定値を受け入れ基準に使わない
- Bolt の取込(ミラー / マージ)は approve 前ではなく review 前に行う。レビュアーは conductor ツリーを読むため、取込前に出すと成果物が申告する実測を再現できない。取込後は `bun run build` を実行し、配送先ツリーの述語で再実測してからレビュー・ゲートへ出す(`git status` が空でも未追跡の投影の更新は証明されない) <!-- cid:code-generation:c1-mirror-and-rebuild-before-review -->
- 既存 codekb がある場合、reverse-engineering は前回スキャンコミットからの差分リフレッシュで実行する。observed commit にはローカル merge でなく origin/main 系譜のコミットを記録し(squash 運用では非祖先化して base 選定が退行する)、差分 base は HEAD の祖先である observed のうち距離最小を選ぶ。旧 intent の節に残る現在時制マーカーは追記前に履歴ラベルへ更新し、履歴節の file:line はその節が宣言する observed commit で照合する <!-- cid:reverse-engineering:c1 -->
- 再現・検証用の scratch スクリプトは repo 外で実行し、audit / record を書くツールの実験では project-root override を scratch に向ける。resolver・path 解決系の段順を変更するときは、env 変数がテスト隔離シームとして機能している既存契約を変更前に棚卸しする — 段順変更の失敗様式はテストの赤ではなく実 record への無音の書込汚染であり、閉包は当該テストを1本実行して実 record の不変量(audit 行数、memory 層と state の md5)が不変であることで確認する <!-- cid:code-generation:c2-env-isolation-seam-inventory -->
- 台帳ファイルのマージ衝突は、マーカー行の貼り合わせでなく3ステージ blob 全文から再構成する。解決後は競合マーカーの機械検査(`<<<<<<<` / `>>>>>>>` / `|||||||`)を独立ステップで通し、構造化ファイルは parse 検証を併用する。追記型の監査シャードをマージした後は同一行の完全重複検査を1手挟み、重複行は後発出現のみ除去する <!-- cid:code-generation:cg-ledger-blob-reconstruction -->

- self-fix は既存コードベースへの incremental brownfield 作業なので walking-skeleton を off とし、units-generation を skip する場合も Issue 単位の unit ディレクトリを作る。 (learned 2026-08-18) <!-- cid:code-generation:c1-issue3029 -->
- pr-convergence の `override` は self-* record では「CLI の `create` を経た生きた PR」専用であり、自 bolt の PR が別 PR に supersede されてクローズされた unit には適用できない — `verifyCurrentPrerequisites`(`plugins/github-pr-convergence/tools/pr-convergence-git-runner.ts:255`、ブランチ照合 :263-266)が checkout ブランチ == PR head ブランチを要求し(逐語 `delivery prerequisite failed: checked-out branch <x> is not the PR head branch <y>`)、それを満たしても `selfEvidence`(同 `pr-convergence-cli.ts:711`、定義 :703)が既存の created epoch を要求する(逐語 `created report is missing; run create first`)。`--unlinked true` は self-* で禁止(同 :662-663)。したがって supersede された unit の配送クロージャは、残存成果物(intent record)を運ぶ Bolt PR を新規に発行して収束させる形をとり、実装の実配送先(PR 番号 / squash SHA / `git merge-base --is-ancestor` の祖先証明)を PR 本文と `code-summary.md` の両方に明記して、収束記録が実装の配送と誤読されないようにする。CLI が mint する report は attestation の content digest に束縛されるため、この区別をレポート本体へ注記することはできない。構造欠落は Issue #3239 (learned 2026-08-19, intent 260809-report-done-kind-split, 監督者裁定 + 梯子 AUTO_DECIDED auto-decision-3eff7541813b9593fc2421a56880fefd) (learned 2026-08-19) <!-- cid:code-generation:c7-260809-override-live-pr -->
- ローカルでフルスイート(`bun run test:ci`)を回すときは `amadeus/spaces/<space>/intents/active-intent` カーソルを退避してから実行する — カーソルが実 intent を指したままだと OTel の one-workspace-per-process 不変量がテスト用の一時 project dir への re-bootstrap を拒否し、`tests/integration/t-approve-batch-presence-guard.integration.test.ts` が**決定的に** 4 fail する(ablation の実測: カーソルあり 2 pass / 4 fail、退避して 6 pass / 0 fail、復帰で再現。単独実行でも再現するためクロステスト汚染ではない)。あわせてテスト由来の `ERROR_LOGGED` が実 record の監査シャードへ着地する(#3032 の残余)。CI はクリーン checkout でカーソルを持たないため緑のまま通り、この赤はローカル専用である — 帰属を誤ると存在しない退行を追うことになる (learned 2026-08-19, intent 260809-report-done-kind-split, 梯子 AUTO_DECIDED auto-decision-3eff7541813b9593fc2421a56880fefd) (learned 2026-08-19) <!-- cid:code-generation:c2-260809-otel-cursor -->
- ステージ成果物のファイル名は、ステージ本文の散文ではなく `run-stage` directive の `produces` が解決した名を正とする — 両者はドリフトする。実測: `build-and-test` のステージ本文 Step 10 は `test-results.md` と書くが、directive の produces は `build-test-results.md` を解決する(`outputs:` frontmatter も本文と同じ旧名を持つ)。本文どおりの名で書くと engine の per-unit / per-stage coverage(required produces の存在検査)が満たされず、次の `next` が同じ run-stage を再発行し続ける。ステージ着手時に directive の produces 配列を実読してから書く (learned 2026-08-19, intent 260809-report-done-kind-split, 梯子 AUTO_DECIDED auto-decision-6999761c4920c87f8e2554f9598beb00) (learned 2026-08-19) <!-- cid:build-and-test:c2-260809-produces-name -->
- §12a が READY を返した後に、その verdict が指す本文を FOLLOW-UP 対応で改訂しない — verdict は改訂前のテキストを指したまま残り、監査者には「レビュー済み」と「現在の本文」が食い違って見える。§12a の contract は FOLLOW-UP で再レビューを消費しないと定めるため、FOLLOW-UP は3つに仕分ける: (a) 測定で閉じられるものは後段ステージの成果物で測り直して閉じる (b) 成果物改訂や別 Issue を要するものは完了ゲートの申し送りへ集約する (c) 当該ステージ内で閉じる必要があるものは READY を取る前に対応して同じ iteration で再レビューする。実測: 本 intent の code-generation iteration-2 は READY + FOLLOW-UP 6件で、うち2件(FR-5 の docs 2面 census、NFR ブロッキング集合の未測定3要素)を build-and-test の `build-test-results.md` で測り直して閉じ、残り4件を `build-and-test-summary.md` の申し送り節へ集約した (learned 2026-08-19, intent 260809-report-done-kind-split, 梯子 AUTO_DECIDED auto-decision-6999761c4920c87f8e2554f9598beb00) (learned 2026-08-19) <!-- cid:build-and-test:c5-260809-followup-routing -->
- conductor が agent-team/mailbox 構成(SendMessage 経由の非同期通信)で動作しているとき、§12a の宣言型 read-only reviewer(amadeus-product-lead-agent / amadeus-architecture-reviewer-agent — tools: Read/Grep/Glob のみで SendMessage/Task 非搭載)を起動すると、最終出力の返送経路がなく無音 idle 化する。発火条件は mailbox 構成であり、同期的 Agent tool 呼び出しでは宣言型でも無音化しない。回避は general-purpose 型 + プロンプト強制 read-only(レビュー作業は Read/Grep/Glob のみ・許可パス限定・Bash/Write/Edit 禁止)+ SendMessage 返送の明示 — これは read-only 規律のツール強制からプロンプト強制への縮退なので、採用時は毎回 diary へ逸脱として記録し監査対象にする。実測: intent 260820-fmc-drift-batch requirements-analysis(宣言型2回無音 idle → general-purpose 構成で iteration 2 READY 到達)。選挙 E-260820-FMC-RA-S13 2-0(GoA 2/3、両票の留保を本文へ反映) (learned 2026-08-20) <!-- cid:requirements-analysis:c3 -->
- cid:build-and-test:registry-merge-recomposition / cid:code-generation:c5-regen-needs-build への追補(宣言時の織り込み — 適用範囲は units-generation の非交差主張): unit write scope の「相互に非交差」主張は、ソース面と生成台帳面(tests/.coverage-registry.json regen、tests/.coverage-patch-allowlist.json 再アンカー等)を書き分けてから立てる — 新規テストを持つ全 unit は生成台帳を書くため、台帳面は構造的に交差する。既存2則は着地時の競合解決を規定するのみで宣言時は未被覆だった。台帳クラスごとに解決手順が異なる点も明示する(registry は regen、patch-allowlist は createSemanticSelector 再アンカーで regen 形では閉じない)。実測: intent 260820-fmc-drift-batch units-generation の §12a iteration 1 BLOCKER(生成台帳の共有書込面が未宣言で非交差主張が falsify)→ iteration 2 で書き分けにより READY。選挙 E-260820-FMC-UG-S13 2-0(GoA 2/2、両票の留保 = 追補形式・適用範囲限定・cid 相互参照・実測根拠併記を本文へ反映) (learned 2026-08-20) <!-- cid:units-generation:c4 -->
- cid:build-and-test:c3-manual-sensor-fire-scope への追補(監査可視性の stage-slug 非対称 — 対象ファイル選定とは別クラス): 手動 `amadeus-sensor.ts fire` の `--stage` 申告値はそのまま audit 行の Stage slug になり、stage 完了の blocking sensor guard(`packages/framework/core/tools/amadeus-state.ts` sensorRowsForStage — Stage slug 厳密一致行のみ読む)には**別 stage 名義の回復 PASSED が構造的に不可視**。あるステージ名義で FAILED が残った artifact は、同じステージ名義の再 fire でしか解決できない。回復手順: (1) 同一 artifact へ正しい stage 名義で再 fire(受理側 3 unit で PASSED 実測) (2) checkout 束縛 receipt(merge facts 未付与の converged report)は conductor checkout では `local head` 検査が構造的に赤のため、原 mint 文脈の bolt worktree(HEAD = receipt.localHead)で再 fire し、audit 行を union 回収する(conductor-only 0 実測なら strict superset copy 可、重複 0・per-shard seq 単調検証必須 — cid:code-generation:c1-shard-monotonic / cid:pr-convergence:prc-headcheckout-output-sweep 準拠)。実測: intent 260820-fmc-drift-batch code-generation ゲート閉包(2026-08-21)— 00:30 の conductor checkout からの code-generation 名義 fire が 4 unit 全て FAILED、pr-convergence 名義の回復 PASSED は guard に不可視、上記手順で guard finding null → approve 成立。方式裁定は梯子 AUTO_DECIDED auto-decision-8410374f1a696726aa91207d3132e24b。重複走査述語: git grep -F 'Stage slug' / 'sensorRowsForStage' / 'finaliseMergedInPlace' → project.md/team.md 0 hit(対照 'coverage-patch-quick' 2 hit)。選挙 E-260821-FMC-BT-S13 2-0(GoA 2x2、両票の留保を本文へ反映) (learned 2026-08-21) <!-- cid:build-and-test:c3 -->
- cid:code-generation:c1-swarm-pr-delivery への追補(per-unit PR 配送の member-loop 未被覆クラス): pr-convergence CLI の merged-arm 最終化 finaliseMergedInPlace(`plugins/github-pr-convergence/tools/pr-convergence-cli.ts:1229-1243`、拒否は finaliseUnitInPlace :1263)は delivery.work.units 全 member をループし、各 member の receipt に attestationBindsDelivery(:994-1002、receipt.pr === ref.number を含む)で**同一 PR 束縛**を要求する。したがって 1 bolt の member units を別々の PR で配送した形(実測: bolt 2 = boundary-three-face PR#3364 / revise-model-commit PR#3363、双方 receipt が memberUnits=[b3f,rmc] を宣言)は構造的に閉じられず、エラーは汎用文言(逐語 `report attestation is missing, tampered, copied, or replayed`)で実体(sibling receipt の pr 不一致)を隠すため誤診断リスクが高い。CLI の fail-closed 自体は「1 bolt = 1 delivery」契約の正当な防衛であり、swarm per-unit PR 配送(暫定運用)側が生む形。予防: swarm 配送で 1 bolt に複数 member unit がある場合、全 member を単一 Bolt PR で運ぶか、member 分割を delivery-planning の Bolt 分割へ反映する。回復: 上記 stage-slug 追補の head-checkout 再測定手順で閉包可能。framework Issue は起票候補(#3378 隣接)— **起票後に本 cid へ Issue 番号を追記し、未起票のまま恒久固定しない**。選挙 E-260821-FMC-BT-S13 2-0(GoA 2x2、subagent-2 留保 = Issue 番号追記運用・c1-swarm-pr-delivery 相互参照を本文へ反映) (learned 2026-08-21) <!-- cid:build-and-test:c4 -->
## Learnings Inbox(未蒸留)

日常の §13 学習・些細なノルム追加はまずこの節へ追記する。定期蒸留ラウンドで本文への昇格(一般化・機械化)または削除を裁定する。蒸留済み本文と未蒸留の具象学習を混在させない。

- 選挙 CLI(v2)の投票者指示には GoA スケールと ballot スキーマを逐語で明記する — GoA は Gradients of Agreement(1/2/3/6=賛成系、4=棄権、5=要議論、7/8=反対系。tally は favor={1,2,3,6} / against={7,8} / 4=abstain で quorum 判定し、2 voter 選挙で abstain 1 票は quorum-short hold)。GoA 2/3/6 は codec が reservation 非空を要求(`amadeus-election-codec.ts` parseResponse 系)。ballot は `kind:"original"` 必須・response オブジェクトへの submittedAt 混入は unknown-field 拒否・top-level submittedAt は秒精度 UTC 必須。実測: intent 260814-open-bug-batch-6 の E-260815-3062-LANDED-FINALIZATION(GoA 誤指示「1-5、5=強い確信」→ 両票 goa:4 = 棄権扱い → quorum-short hold → amend 往復)と E-260815-U3-NEWFILE-DEVIATION(スキーマ拒否3回 → decode デバッグで特定)。あわせて単一 question 選挙の hold→再 tally は #3077 の CLI バグで commit 不能(store 直接 commit で回復した一次記録は intent record)(learned 2026-08-15, 梯子 AUTO_DECIDED auto-decision-ee0554ef4a8346d1b99491f87b408b07) <!-- cid:code-generation:election-v2-ballot-contract -->
- merge queue 投入済み PR のブランチは GH006(protected branch hook)で push 不可 — queue 投入後に record 訂正・report 再 mint が必要になった場合は conductor record を正本として更新し、本線への配送は次の record checkpoint(または最終 checkpoint)で行う。queue からの dequeue はマージ承認の再取得を要するため、record-only の訂正で dequeue しない。実測: PR #3092(2026-08-15、GH006 逐語「Branches that are queued for merging cannot be updated」)(learned 2026-08-15, 同上梯子) <!-- cid:code-generation:queue-immutable-record-canonical -->
- cid:pr-convergence:lcov-type-annotation-asymmetry への追補: bun の合流 lcov が DA:0 の幽霊未カバー行として報告するのは複数行型注釈だけでなく、**関数ブロック内の連続コメント行**も同族 — patch coverage gate がコメント行を UNCOVERED に挙げた場合、位置移動では消えず(ブロック内のどこへ置いても追従)、行そのものの削除(1行化でも不足のことがある)だけが閉じる。実測: PR #3081 の amadeus-sensor-pr-convergence-report-format.ts で 3行コメント→hoist→1行化→削除の3往復(UNCOVERED :381-383 → :379-381 → :381 → 解消、各 run の Patch coverage gate ログ転記)(learned 2026-08-15, 同上梯子) <!-- cid:code-generation:lcov-comment-line-asymmetry -->
- bun の lcov は複数行型注釈の継続行を非対称に扱う — targeted 実行(`bun test --coverage` 単体)では DA レコード自体が出ないが、フルパイプライン(`coverage:ci` の合流 lcov)では DA:0 の未カバー行として現れる。patch coverage gate がこの型注釈行だけを赤にした場合、テスト追加では閉じられない — inline の複数行型リテラルを名前付き interface / type へ切り出して行そのものを消すのが正しい閉じ方。実測(PR #2932、いずれも CI の Patch coverage gate ログ `gh run view <run> --job <job> --log | grep UNCOVERED` からの転記、計 9 行 = 5 行 + 4 行の2回分): (1) run 31586616801(head 9dcf55288)の UNCOVERED 55 行中、型注釈継続行は `plugins/pr-convergence/tools/pr-convergence-cli.ts:560-562`(3 行、`existingReportKind` の inline 戻り値型)と `plugins/pr-convergence/tools/pr-convergence-git-runner.ts:54-55`(2 行、`selfOutputPaths` の inline 戻り値型)→ 各 interface(`ExistingReport` / `SelfOutputPaths`)へ切り出しで消滅。(2) run 31595357018(head 7b2e78b4b)の UNCOVERED は残余 4 行 = `packages/framework/core/tools/amadeus-state.ts:1760-1763`(`new Map<...>` の inline 値型)→ `interface TerminalSensorVerdict` へ切り出しで消滅し、次 run で uncovered 0 を確認(learned 2026-08-12, intent 260811-pr-convergence-gate pr-convergence, semi 梯子 AUTO_DECIDED) <!-- cid:pr-convergence:lcov-type-annotation-asymmetry -->
- pr-convergence report はマージ後でも `kind: landed` で生成できる — `landed` は第一級 verdict であり、`converged` はマージ前専用(`plugins/pr-convergence/tools/pr-convergence-predicate.ts` の定義: converged は mergeStateStatus CLEAN 等を要求、landed は「マージ済みは収束させる状態でなく記録する事実」)。report の check rollup はマージ後は **merge commit の check-runs** を読むため、PR と無関係な post-merge workflow の失敗で偽 FAILURE 化しうる(実測: Issue #2925(Metrics Snapshot の merge queue 非互換)により rollup PENDING→FAILURE→是正後 SUCCESS の全経路を観測。FAILURE の一次証跡は run 31562759226 の **attempt 1**(`gh api repos/amadeus-dlc/amadeus/actions/runs/31562759226/attempts/1/jobs` で Metrics Snapshot job 94010255732 = failure を再取得可能)— 失敗ジョブの再実行により最新 check-runs では success に置換されているため、attempt 指定なしでは再導出できない)。rollup 赤は PR 自体の欠陥と即断せず、merge commit の failing check を帰属してから対処する (learned 2026-08-12, intent 260812-tla-proof-receipt code-generation, semi 梯子 AUTO_DECIDED `auto-decision-7eadb3d0e5a28b0a04d2457e6e0bfb8a`) <!-- cid:code-generation:c1-landed-rollup-attribution -->
- cid:reverse-engineering:E-XBB-RE-S13-c2(xrev scan mode の currency 判定)への追補: **患部の表現形式そのものを変える移行が review 断面と observed の間に挟まっている場合、currency 条件は構造的に不成立**であり、行番号の再解決では救えない — その intent は xrev differential scan mode を採らず通常の差分スキャンとし、クロスレビュー verdict は背景としてのみ用いて全主張を observed 断面で取り直す。既存則は免除条件を「`review..observed` の実 diff と被引用パス集合の交わりが空」と定めるが、移行によって台帳・成果物のスキーマ自体が変わった場合は、verdict が引く行番号・エントリ形式が現行スキーマへ写像できず、diff の交差判定以前に照合の土台が失われる。判定手順: (1) review 断面以後に患部の表現形式(スキーマ・セレクタ形式・ファイル様式)を変える移行 PR が着地しているかを実測 (2) 着地していれば currency 不成立と確定し、その旨と根拠 PR を成果物へ明記して通常スキャンへ切り替える。実測: intent 260811-allowlist-semantic-audit の RE で、Issue #1622 のクロスレビュー2名 CONFIRMED(2026-07-28)以後に PR #2127 が `tests/.coverage-patch-allowlist.json` を絶対行番号ピンから意味的セレクタ(`function` + `fingerprint` + `anchorLines` + `targetLines`)へ全面移行しており、verdict が引く行ピンが現行スキーマに存在しないため通常スキャンを選択した(measurement ref: base `ce3c3ccfd` / observed `854692fd7`、記録は `codekb/amadeus/re-scans/260811-allowlist-semantic-audit.md` §1) (learned 2026-08-11) <!-- cid:reverse-engineering:c5-xrev-currency-schema-migration -->
- cid:requirements-analysis:absence-claim-grep-verify への追補: **不在主張の反証確認に使う grep 述語自体が異常終了しうる** — 空出力を 0 hit の根拠にする前に exit code を確認し、複数の選択肢を `|` で連ねた複雑な選言述語は分割して再実測する。ローカルの `grep` が ugrep ラッパである環境では、選言が長い正規表現が `ugrep: error: ... exceeds complexity limits` で **exit 1** を返し、出力が空になる。この空出力は「一致なし」と見分けがつかないため、不在主張がエラーの上に build される。既存則は不在主張に反証確認を求めるが、その反証確認に使う述語の健全性は未被覆だった。実測: intent 260811-allowlist-semantic-audit の RE で、Developer scan が5選択肢の `grep -rniE` 1本の空出力を「0 hit」と記録 → Architect の独立再実行で exit 1 の異常終了と判明 → 述語を2本へ分割し `git grep` で再実測して 0 hit を確定(結論自体は不変。記録は `codekb/amadeus/re-scans/260811-allowlist-semantic-audit.md` §2「P2c の訂正」)。`cid:reverse-engineering:c4-control-byte-drafting` が記す「grep 系ラッパの無音脱落」と同族 (learned 2026-08-11) <!-- cid:reverse-engineering:c6-absence-predicate-exit-code -->
- 測定不能な受け入れ基準を測定可能へ是正するとき、**新しい測定条件が上位の裁定の経路を構造的に不能にしていないか**を、裁定の全選択肢へ適用して確認してから書く — 曖昧さの除去を急いで条件を強くしすぎると、裁定が認めた是正方式の一部が「基準を満たせない」を理由に事実上禁止され、無申告のスコープ縮小と同じ結果になる。判定手順: 是正後の AC を、裁定が認める各経路(A/B/C 等)へ順に当てはめ、どれか一つでも構造的に満たせなくなるなら AC 側が誤り。解消は条件の撤回ではなく**帰属付きの条件**へ書き換えること(禁止する対象を「すべての X」から「どの正当な経路にも帰属しない X」へ狭める)。既存の cid:requirements-analysis:exemption-clause-must-not-substitute は免責条項が実質基準を代替する向き(基準が緩すぎる側)を縛るが、その鏡像である**基準が強すぎて上位裁定を潰す側**は未被覆だった。実測: 260811-allowlist-semantic-audit の requirements-analysis で、§12a iteration 1 の MAJOR「差分が説明可能であることは測定不能」を是正して「免除対象行集合の増加行が 0 件」を課したところ、裁定 Q2=C が主要経路として認める「セレクタを真の対象へ張り直す」是正が新解決先の行を免除集合へ入れる以上、構造的に満たせず、AC を満たす唯一の道が全転位エントリ削除となって Q2=C を削除一択へ縮小した。iteration 2 が BLOCKER として捕捉し、「増加行は張り直し元エントリに帰属する行のみ、どのエントリにも帰属しない増加行が 0 件」というエントリ帰属付きの集合条件へ書き換えて閉包した (learned 2026-08-11) <!-- cid:requirements-analysis:c3-measurable-ac-must-not-void-ruling -->
- 上流入力ヘッダに artifact 名を書けることと、その artifact から**本 intent の主張を引ける**ことは別である — 差分リフレッシュの RE が「レビュー済みで無変更」とした codekb 面には本 intent の節が存在しないため、そこから本 intent の事実を引く記述は cid:requirements-analysis:mechanism-cite-verify-at-draft 違反(装飾参照)になる。起草手順: consumes の各面について (i) 本 intent の RE がそれを更新したか「レビュー済み無変更」としたかを re-scan record で確認し (ii) 更新面からのみ本 intent の事実を引き (iii) 無変更面からは既存の一般記述のみを前提として受け取る、と成果物へ明記する。既存の cid:code-generation:artifact-upstream-inputs-header は装飾トークン(本文で使わない参照)を禁じ、cid:nfr-design:body-derivation-before-header は本文の実参照から転記する順序を定めるが、**参照先に引ける内容が存在するかの確認**は両者の隙間だった。実測: 260811-allowlist-semantic-audit の requirements-analysis で business-overview.md / code-structure.md を「が記すとおり」と引いたが、両ファイルは本 intent の RE が無変更とした面で 260811 の節を持たず(product-lead reviewer が 0 hit を実測して MAJOR ×2)、consume 3 面のうち本 intent の節を持つのは architecture.md のみだった (learned 2026-08-11) <!-- cid:requirements-analysis:c4-consume-header-is-not-citable-content -->
- 散文フィールドに対する機械述語の初回適用で不一致率が高く出たときは、第一容疑を「対象の腐敗」ではなく「述語が現実の語法をカバーしていないこと」に置き、実サンプルの逐語読解で切り分けてから是正工数を見積もる — 逆に置くと、正しい対象を壊す是正へ進む。実測: 260811-allowlist-semantic-audit の code-generation で、免除台帳 623 件への構文クラス述語が転位 167 件を返したが、うち 125 件は述語の不足由来だった(台帳は spawn 到達性を主張し、述語は import.meta.main 分岐への構文上の包含を要求していた)。判定不能 417 件のうち 346 件も同様で、reason は正しく型のみと述べ実クラスも一致していた。述語を疑わずに是正へ進んでいれば、正しい免除を大量に壊していた (learned 2026-08-11) <!-- cid:code-generation:c4 -->
- 散文から主題を抽出して機械判定する設計は、宣言を prose から parse する限り prose 解釈の問題を持ち越す — 宣言は推論ではなく入力にする(台帳・設定に optional な構造化フィールドを足し、そのフィールドだけを機械照合する)。optional にすれば、フィールドを**省略した**既存エントリだけが無検査のまま緑で始まり、見直したエントリから opt-in するラチェットになるため、全件強制による「検査できない宣言」の増殖も避けられる。**省略と不正値は区別する** — 空値・閉語彙外の値・非文字列は malformed としてゲートを失敗させ、無検査へ落とさない(省略のみが opt-out であり、書き損じは fail-closed)。実測: 260811-allowlist-semantic-audit で 4 つの設計(構文クラス述語での全数分類 / 全件規約化 / 関数名照合 / prose から宣言を parse)がいずれも偽陽性で否定され(それぞれ述語不足 125 件 / AC 両立不能 / 偽陽性 63.9% / 発火 20 件中 8 件以上が偽陽性)、selector.class をデータフィールドとして持たせる形で初めて偽陽性が構成上生じなくなった。閉語彙は機械判定できるものだけに限る — 判定できないクラスを宣言可能にすると、宣言だけで通るエントリが増えてゲートの担保が増えない (learned 2026-08-11) <!-- cid:code-generation:c-decl-input -->
- 免除・waiver・例外の是正で「その例外がまだ必要か」を判定するときは、散文の根拠ではなく計測値で決める — coverage 免除なら lcov の DA が正のヒットを持つかを実測し、被覆済みならエントリを削除、未被覆ならセレクタを真の対象へ張り直す。実測: 260811-allowlist-semantic-audit の FR-2 で、RE が確定させた転位 18 件のうち 5 件は reason が「防御的 catch だから免除」と述べていたが DA 実測では被覆済みで、散文だけで判断していれば不要な免除を残していた(結果は張り直し 11 件 / 削除 7 件)。あわせて、是正前後の免除対象行集合を機械 diff し、増加行・減少行の**両側**について未帰属が 0 件であること(未帰属の増加行 0 件**かつ**未帰属の減少行 0 件、どちらかが非0なら非0終了)を述語で検査する — exit 0 は免除が緩む方向へ増えても返るため、実効の担保は帰属条件の側にある。増加行だけを見ると「説明のつかない免除の消失」(必要な免除の巻き添え削除)が素通りする (learned 2026-08-11) <!-- cid:code-generation:c-measure-not-prose -->
- テストで intent autonomy projection を fixture へ実書込するとき(applyProductionAutonomyMode)、seed の state fixture は `Construction Autonomy Mode` フィールドを既に持つもの(例: tests/fixtures/state-construction.md)を使う — strict field writer は不在フィールドの挿入を拒否するため、フィールドを持たない seed(state-mid-ideation.md 等)では宣言が ok:false になる。実測: t528 の semiAutonomyProject() 構築時(intent 260814-t528-ambient-isolation code-generation、PR #3000)。あわせて OTel の one-workspace-per-process 不変量により、fixture ごとに mintHumanPresence 前に resetOtelPerProject() が必要 (learned 2026-08-14, semi/full 梯子 AUTO_DECIDED auto-decision-6593ffc52ca726d25e9e29da32e1c165) (learned 2026-08-14) <!-- cid:code-generation:c2 -->
- xrev differential scan mode の本則 cid:reverse-engineering:c1-xrev-scan-mode は 2026-08-12 のノルム蒸留(PR #2919, bd567fd1b)で project.md から削除され、currency 判定の追補(cid:reverse-engineering:c5-xrev-currency-schema-migration)だけが本 Inbox に残存している — 追補が引く本則が本文に存在しない「空洞化」状態。scan mode 採否の判断は現状 c5 と git 履歴(bd567fd1b 以前)からしか導出できない。次回蒸留ラウンドで (a) 本則の再導入(原理化) (b) 追補ごと削除 (c) 現状維持のいずれかを裁定する(発見: E-260814-T245-RE-S13 の split 票。不在の実測: `grep -n "xrev-scan-mode\|c1-xrev" amadeus/spaces/default/memory/project.md amadeus/spaces/default/memory/team.md`(固定文字列 2 選択肢の basic regex、大小文字区別、対象 tree = 本 PR 追記前の origin/main 断面 5f6b5bf97)→ 出力 0 行・**exit 1**(grep の exit 1 は「エラーなく不一致」— エラー時は exit 2。述語は 2 本に分割せず単純選言のため complexity limit 非該当)。存在側の対照: 同述語を bd567fd1b^ の project.md へ適用すると 7 行一致(`grep -c` 転記)。learned 2026-08-14, intent 260814-t245-origin-fixture reverse-engineering, 選挙 hold のユーザー裁定) <!-- cid:reverse-engineering:xrev-scan-mode-cid-hollowing -->
- §12a reviewer にスコープ外パスの読取をプロンプトで許可しない — spot-check は reviewer-runtime の check-read 契約(integration ID が現行 artifact に実在・owner evidence が passed consume・一意一致)に適合する場合のみ成立し、それ以外の追加事実は conductor が attested context としてプロンプトへ要約供給する(一次記録の所在を併記)。実測: 260814-fmc-macos-provider の code-generation で、conductor が選挙 record の読取を許可 → reviewer が誠実に scopeTranscript へ記録 → complete-review が `spot-check request bypassed check-read` で verdict を無効化(fail-closed が正しく作動)。observe-quality(repair)→ scope 厳守の再 invocation で READY を確定した (learned 2026-08-14, intent 260814-fmc-macos-provider code-generation, ladder AUTO_DECIDED auto-decision-8e686e9d32a96e0298a9abcf159333f0) (learned 2026-08-14) <!-- cid:code-generation:c3 -->
- push 前の patch coverage 往復は `coverage-patch-quick` の advisory 判定を標準とし、フル `coverage:ci` はゲート直前の最終確認1回に限る。ローカルでフル `coverage:ci` を回すときは CI と同等の `-P 4` を付ける(`.github/workflows/ci.yml:466` / `:550` 逐語 `bun run coverage:ci -- -P 4`)。実行中は他の重い作業を並行しない — coverage single-owner(`cid:code-generation:c1-coverage-single-owner`)と load-sensitive 帯(#1331 / #1326、`tests/run-tests.ts:56-58`)に従う。quick は advisory であり、blocking gate(CI の Patch Coverage Gate / Project Coverage Gate)の代替ではない。完了時 exit は gate verdict に依らず 0(`EXIT_ADVISORY`、`plugins/coverage-patch-quick/tools/coverage-patch-quick-cli.ts:254-255`)。実測: Issue #2933、PR #2965(MERGED)。CI job 94095568607 を本起草時に再取得(`gh api repos/amadeus-dlc/amadeus/actions/jobs/94095568607`)し、`Generate coverage reports` は 2026-08-12T11:15:45Z→11:26:48Z(11 分 03 秒)、`Patch coverage gate` は 11:26:48Z→11:26:51Z(3 秒)。本ワークスペースでは `amadeus/config.json` の `plugin.activation.names` に有効化済み。learned 2026-08-14, intent 260814-coverage-quick-norm code-generation, full 梯子 AUTO_DECIDED <!-- cid:code-generation:coverage-patch-quick-pre-push-standard -->
- cid:reverse-engineering:c6-absence-predicate-exit-code への追補: ローカル grep が ugrep ラッパの環境では、ERE の \b(語境界)を含む述語が一致0件・exit 1 で無音終了する — 選言の複雑度超過(既存則)とは別の第2トリガ。実測(intent 260814-ambient-error-sink RE): git grep -n -E "\bhandleReport\(" -- '*.ts' → 空出力 exit 1 / \b なしの同述語 → 4 hit exit 0。全数棚卸しでは \b を使わず、部分文字列述語+括弧バランス抽出等の機械走査で採る (learned 2026-08-14, semi/full 梯子 AUTO_DECIDED auto-decision-5ec8115e0a02d2a8121e1cb3225573b9) (learned 2026-08-14) <!-- cid:reverse-engineering:c6-ugrep-word-boundary -->
- packages/framework/core/tools/amadeus-orchestrate.ts(ほか model-map.json の entries に implPath として載る実装ファイル)を変更する PR は、(a) amadeus/spaces/default/specs/tla/model-map.json の実装ハッシュピン(是正: bun plugins/formal-model-check/tools/amadeus-sensor-model-completeness.ts updateModelMap --impl-only — モデル・cfg 不変時)と (b) tests/.coverage-patch-allowlist.json の意味的セレクタ(署名行など anchor に含む行が変わった場合。是正: gate 自身の createSemanticSelector で再アンカー)の2台帳を同一変更で resync する。怠るとフルスイートが SOURCE_DRIFT(formal-verif 系)/ fingerprint 不一致(t535)で赤化する。実測: intent 260814-ambient-error-sink build-and-test、1回目フルスイート exit 12・Failed files 12 が全件この2クラス → resync 後 exit 0(commit ee1394489) (learned 2026-08-14, semi/full 梯子 AUTO_DECIDED auto-decision-03777c33d11ba06450639cb36094fffc) (learned 2026-08-14) <!-- cid:build-and-test:bt-ledger-resync -->
- pr-convergence の Delivery Bolt authority は、self-fix / self-refactor / self-document の degrade スコープ(units-generation・delivery-planning SKIP)では engine-singleton であり、record の construction 配下の unit ディレクトリが**ちょうど1つ**であることを要求する(`plugins/pr-convergence/tools/pr-convergence-presentation.ts:117-129` — stage slug 以外の dir を数え `units.length !== 1` は MISMATCH。runtime projection は `amadeus-runtime.ts computeDeliveryBoltProjectionOutcome` が amadeus-state.md から digest 束縛で生成)。したがって複数 Issue を1つの degrade intent に載せると、2つ目の unit dir を作った時点で全 unit の report mint(create / report / override)が構造的に不成立になる。複数 Issue はそれぞれ別 intent(1 intent = 1 unit)へ分けるか、units-generation / delivery-planning を EXECUTE する scope を選ぶ。実測: intent 260814-autonomy-stop-fixes(#3016 + #2974 の2 Issue で開始)の code-generation で、unit issue-2974-error-arm-boundary の PR #3037 create は成立したが、2 unit 目の追加が authority を壊すことを事前実読で検出し、#3016 を別 intent へ分離する方針とした (learned 2026-08-14, semi 梯子 AUTO_DECIDED auto-decision-535a8104f51308ad1ffd4a4994d5a374) (learned 2026-08-14) <!-- cid:code-generation:oq-singleton -->
- cid:build-and-test:bt-ledger-resync への追補: 新規テストファイルを追加する PR は `tests/.coverage-registry.json` の regen(`bun tests/gen-coverage-registry.ts`)を同一変更で同梱する — CI の gen-coverage-registry freshness テスト(enumerated universe と committed registry の diff)が赤化する第3の台帳クラス。実測: intent 260814-plugins-rename-drift で PR #3051(t2996 追加)と conductor 統合断面(t2996+t2997 合流)の両方が赤化し、regen 同梱で解消。再導出: 赤の一次証跡は `gh api repos/amadeus-dlc/amadeus/actions/jobs/94783218761`(run 31805449586、head 2de80e130 の親 5bbf41a59 断面)のログ `FRESHNESS DIFF FAILED`。検出述語はテスト内の `bun tests/gen-coverage-registry.ts --check`(committed `tests/.coverage-registry.json` と、テストファイル群から列挙した enumerated universe の diff — 列挙元はツール自身の走査)。是正は同ツールを引数なし実行して registry を再生成し、regen 前後は同 `--check` の exit code(1→0)で比較(semi 梯子 AUTO_DECIDED auto-decision-5cb687b1e0d955afa96d48c86c30dc7b) (learned 2026-08-14) <!-- cid:build-and-test:c1 -->
- Bolt 実装の検証は push-first を既定とする — commit ができ次第ただちに origin へ push して PR を作成(または既存 PR へ反映)し、フルスイート・coverage 等の重い検証は push 後にリモート CI と並列で行う。ローカル完走を push の前提にしない(pr-convergence ステージの Push-first 節「Push-first changes the ordering of validation, never the approval boundary」の適用 — remote write の approval boundary(梯子)は不変で、変わるのは検証の順序だけ)。ローカルで先に回してよいのは秒〜十秒級の即時検査(typecheck・対象テスト単体)まで。**既存則との優先関係**: `cid:code-generation:coverage-patch-quick-pre-push-standard`(上記)の「push 前の patch coverage 往復」という時点指定は、本則が**時点についてのみ**置き換える — `coverage-patch-quick` の advisory 往復は push 後に CI と並列で回す(quick を advisory 標準としフル `coverage:ci` をゲート直前1回に限る部分・single-owner/load-sensitive 規律は不変。push 後にローカルで遅い検証を走らせる場合も `cid:code-generation:c1-coverage-single-owner` の直列化規律に従う)。リモート CI は並行検証の**既定の観測先**であって免除装置ではない — pr-convergence 正本の終了条件どおり、CI green は走行中・失敗したローカル検証を免除せず、ループは required check set の green **かつ**該当ローカル検証の合格で終端する。なお push 前完走の許容閾値は正本の「見積り 3 分未満」に対し本則は「秒〜十秒級」とより厳格な project 上乗せであり、次回蒸留の統合時にこの数値差も一本化する。この時点変更の根拠はユーザーの直接是正指示(本セッション実 HUMAN_TURN ×2)であり、旧則の梯子裁定より上位。次回蒸留ラウンドで 2 bullet を統合する。実測: intent 260814-autonomy-stop-fixes / 260814-park-provenance の2 Bolt 連続で「ローカルフルスイート+coverage 完走後に push」の直列運用に倒れ、フルスイート複数周(各10分超)+ coverage-patch-quick(10分 timeout)が critical path 化し、ユーザーが2度是正指摘(2026-08-14 本セッション HUMAN_TURN)。是正後は PR #3053 を検証前に作成し CI と並列化した (learned 2026-08-14, semi 梯子 AUTO_DECIDED auto-decision-d068931095fc683eafec00433592206b) (learned 2026-08-14) <!-- cid:code-generation:push-first -->
- pr-convergence の create / report は record が PR head ブランチのチェックアウト内に実在することを前提とする(`plugins/github-pr-convergence/tools/pr-convergence-git-runner.ts:166` の `verifyCurrentPrerequisites` が record パスを git cwd に使う)— degrade スコープの Bolt では record checkpoint を bolt ブランチへ同梱コミットしてから created epoch を mint する。PR が既に存在する状態での create 再実行は既存 PR を読み戻して created report を mint する公式リカバリ経路。attestation 失敗(audit tool の相対パス解決)はソース断面 plugins/ からの起動が原因で、self-install 投影(.claude/plugins/...)から再実行すると解消する。実測: intent 260814-priority-bug-batch code-generation、PR #3076(learned 2026-08-15, full 梯子 AUTO_DECIDED auto-decision-1d8d3a85c84aef8e19a330116b14e41b) (learned 2026-08-15) <!-- cid:code-generation:c2-pr-record-in-head-checkout -->
- Project Coverage Gate の相対条件(許容 0.02pp)は、これまで in-process import されていなかった大きなソースファイルを1本のテストが import しただけで構造的に赤化する — ファイル全体が lcov 母集団へ入り、未カバー行が分母を膨張させるため(実測(対象 tree = PR #3076 初回 head 984d6206e、転記元 = run 31854003601 の Project coverage gate ログ逐語「current 94691/103833 (91.1955%), merge-base 94348/101115 (93.3076%), delta -2.1121pp」): 3847 行 = `wc -l packages/framework/core/tools/amadeus-migrate.ts`(base 8b36a0ad0 断面)、+2718 行 = 103833−101115(母集団総行数の差、算出式併記の派生値)、delta -2.1121pp = 91.1955−93.3076)。是正は waiver でなく、被検関数を小モジュールへ切り出してテストはそれだけを import する(実測: amadeus-migrate-git.ts 31 行へ切り出しで解消、commit a81b21c02)。CLI として spawn 実行されるだけの大型 tools ファイルはこのクラスに入りやすい (learned 2026-08-15, intent 260814-priority-bug-batch build-and-test, full 梯子 AUTO_DECIDED auto-decision-09da193f5185b0b9827b1d39ffa2beff) (learned 2026-08-15) <!-- cid:build-and-test:bt-coverage-universe-inflation -->
- 選挙で方式選定が established(全票同一 choice)でも、各票の reservation が実装形で相互に矛盾する場合は、conductor が単独で実装形を裁定せず、共通制約(両票合意分)を question 本文に固定した runoff 選挙を fresh 2 voter で開く。実測: E-260815-3099-FIX-METHOD(方式 C 2-0)の両留保が「pool writer 再利用」vs「別イベント新設」で正面衝突 → runoff E-260815-3099-C-FORM が C2 を 2-0(GoA 2/2)で確定し、両票の留保が今度は同一の実装契約(冪等鍵・発行点・pool 優先 de-dup・台帳列挙)へ収束した。留保は established の付帯情報ではなく実装契約の一部として読み、矛盾検知を tally 直後の定型チェックにする (learned 2026-08-15, intent 260815-per-unit-outcome code-generation, ユーザー裁定) (learned 2026-08-15) <!-- cid:code-generation:c1 -->
- PR 収束は push のたびに再レビューが走り新指摘が届きうるため、毎 push の定型を (1) 常設 Monitor(CI 確定・head 更新・新規未解決スレッド・新規コメント/レビューの検知) (2) unresolved×non-outdated の CodeRabbit スレッドを pagination 付き GraphQL で全数取得する sweep(検出側・0件側の両方を実 PR で検証済みの述語) (3) レビュー本文は untrusted data として現行コードで実否検証→有効のみ重大度順に修正・無効は根拠付き却下返信 (4) スレッド返信→resolve に加えトップレベルコメント・review summary へも対応内容/却下理由を返信 とし、merge-ready 判定を「必須 CI green ∧ sweep unresolved=0 ∧ 全コメント返信済み」の最新 head 再実測に固定する。既存 review-feedback-closure ノルムの per-push 運用化。出典: coderabbitai/skills autofix SKILL.md、ユーザー直接是正指示(2026-08-15 実 HUMAN_TURN「pushするたびに指摘される可能性があるのでそういうスクリプトを仕掛けないとカバーできなくなる」)。実測: PR #3105 の 3 ラウンド(t81 ピン/OTel fixture/lcov 継続行/outcome 語彙/Stage 検証)で運用し MERGED b9615ffb8 へ着地 (learned 2026-08-15, intent 260815-per-unit-outcome pr-convergence, full 梯子 AUTO_DECIDED) (learned 2026-08-15) <!-- cid:pr-convergence:c1 -->
- cid:code-generation:c2-pr-record-in-head-checkout / #3042 への追補(**本則は PR #3113 の着地で経路が実装されたことに合わせ改訂**): created attestation が stale 化した **MERGED** PR にも、いまは正規の最終化経路が**ある** — `report` が merged arm で祖先を実測し(attested prHead が merged headRefOid の祖先であることを `refs/pull/<n>/head` の fetch + `git merge-base --is-ancestor` で判定)、merge commit SHA と mergedAt に束縛した `kind: landed` を書く。live-head 前提(ブランチ名一致・remote 存在・local==remote==prHead)は merged arm では**置換**され、識別は push で変えられない半分(Intent/Bolt/Unit/PR)+ 祖先証明が担う。`create` は自 delivery の head が MERGED PR を持つとき loud 拒否し、`report` を指す。**歴史的経緯(改訂前の挙動、再発防止の根拠)**: 修正前は status が landed(exit 0)を返す一方で report は stale 拒否(是正指示が push+create 再実行)、create 再実行は MERGED PR を reuse せず**新規 PR を誤作成**し(実測: #3080 の head 再 push で #3109 が開いた — 即クローズ)、override は valid な created attestation を要求して閉路になっていた。**予防知識(いまも有効)**: 「PR がまだ OPEN **かつ merge queue 未投入**のうちに、verdict 後の record checkpoint を積んだら push + create 再 mint を済ませる」— queue 投入後は GH006 で push 不可(cid:code-generation:queue-immutable-record-canonical)。MERGED 後に一般の push+create 指示を適用してはならない点も不変で、いまはその誤用を CLI 自身が拒否する。実測: intent 260814-open-bug-batch-6 の 5 unit(#3080/#3081/#3086/#3089/#3092)全てで旧挙動を再現(resume 断面 2026-08-15)、経路の実装と落ちる実証は intent 260815-stale-epoch-landed / PR #3113(t3110) (learned 2026-08-15, full 梯子 AUTO_DECIDED; 改訂 2026-08-15 PR #3113) <!-- cid:pr-convergence:c1-2 -->

- audit shard の union(マージ・rebase・レーン回収)後の機械検査は「同一行の完全重複」に加えて「per-shard の seq 単調性」を必須にする — 行完全一致 dedup はレーン間で再 mint された同 seq 行(内容違い)を素通しし、fatal-latch probe の赤で顕在化するまで単調性破壊が潜伏する。是正は出現順保持の単調 seq 再割当。実測: 2026-08-16 のインシデント(seq 37-39 ×3 ブロック、#3128 経由で main へ混入 → 全 worktree 修復)と、本 intent の record 回収 5 回(61/52→67/91 行、いずれも union + 再割当 + 単調検証で閉包)。cid:code-generation:cg-ledger-blob-reconstruction の拡張 (learned 2026-08-16, intent 260815-rfc-autonomy-modes code-generation, 梯子 AUTO_DECIDED auto-decision-d303437963cad4c61f6d591537fbc07e) (learned 2026-08-16) <!-- cid:code-generation:c1-shard-monotonic -->
- FD の「refine しない」pin は、同一コードブロック・同一文書内の型・不変条件と両立するかを起草時・レビュー時に検査する — pin と同居する SummaryBuildError 型と「生成失敗は SummaryBuildError を経て警告フィールドへ」不変条件が、pin 署名(素の SummaryDoc 戻り)では実装不能だった(C9、E-260816-C9-SIGNATURE 1-1 tie → ユーザー裁定 B=強制適応追認)。同型の R-6「本 unit」多義も 1-1 tie → ユーザー裁定 B(E-260816-R6-FINGERPRINT)。pin の主語(モジュール/unit/ファイル集合)と例外条件を pin 自身に明記する (learned 2026-08-16, 同上) (learned 2026-08-16) <!-- cid:code-generation:c2-fd-pin-self-contradiction -->
- swarm 並列実装 + PR 配送の経路では、unit の CG 成果物が record に揃っても §12a reviewer verdict が永続化されず、後段で engine の review_only 回復(unit 直列)が必要になる — 実測: 本 intent の回復パスの reviewer subagent 実行時間(conductor 側 usage 記録 duration_ms からの転記、2026-08-16)= d6 173.7 秒 / grant-ceremony 268.8 / merge-provenance 140.4 / presence-detection 134.3 / recommendation-core 357.6 — 5 unit 平均 215 秒(派生値、算出式 Σ/5。一次転記と短縮試算は Issue #3143 本文)。swarm 配送では各 unit の PR 収束時(または record 回収時)に §12a レビューを同時に確立し、verdict を record へ載せてから次 unit へ進む。並行化提案は Issue #3143 (learned 2026-08-16, 同上) (learned 2026-08-16) <!-- cid:code-generation:c3-swarm-verdict-at-delivery -->
- units-generation の LOC 見積りは、FD が必須化する監査証跡・エラー処理(Result 化・全 kind 網羅)・テストの行数を含めて較正する — 実測(見積の転記元 = unit-of-work.md:7,14,18 の見積列、実績の転記元 = 各 unit の code-summary.md 実装 summary の行数申告。倍率 = 実績/見積の派生値): U8 completion-report ~120 行見積 → 非テスト実装 256 行 = 7+197+52(2.1 倍)、U10 merge-provenance ~60 行 → 156 行 = 74+67+15(2.6 倍)、U12 docs-norms ~150 行 → mode-matrix 113 + t3116 テスト 184 + norm-revision-drafts 126 + 対訳 doc 群(§12a iteration-1 NIT の列挙より)。いずれも設計違反ではなく FD 必須要素の未計上 (learned 2026-08-16, 同上) (learned 2026-08-16) <!-- cid:code-generation:c4-loc-calibration -->
- coverage registry / ratchet の generator は dist の投影を読むため、マージ取込直後の regen は必ず `bun run build` の後に行う — stale dist のまま regen すると他レーンの新規関数が enumeration universe から欠落し covered 数が無音で退行する(実測: PR #3130 の base 競合解消時(2026-08-16、merge 後 tree = のちの 26d04cd08 断面)、`bun tests/gen-coverage-registry.ts` の coveredByClass 出力転記で stale dist 時 function 186 → `bun run build` 後の再実行で 187。欠落関数は theirs 側 registry との covered 集合差分で verifyBatchApprovalPresence と特定)。同族: 本 tree の self-install(.claude/tools)も merge 後は build 前提 — stale な election CLI が新 config スキーマを拒否した実測あり。cid:code-generation:c1-mirror-and-rebuild-before-review の適用面拡張 (learned 2026-08-16, 同上) (learned 2026-08-16) <!-- cid:code-generation:c5-regen-needs-build -->
- 同一の条件述語が複数箇所へ手書き複製されている面を修正するときは、修正前に全複製箇所を grep で列挙して 1 定義へ集約する — 一部だけ直すと失敗が下流へ移動し「直ったが別の段で落ちる」形になる。実測: #3077(選挙の preservedResultDigest)は生産・検証・report 期待の 3 箇所が同じ述語を持ち、計画が名指した 2 箇所だけの修正では tally は成功するが report が stale-directive で落ち指令ループが terminal へ到達しなかった。実装中に第3の箇所(matchesReportExpectation、amadeus-election.ts:579)を発見して runPreservedDigest(:417)へ集約(呼び出し: isCommittedRun :436 / tallyElection :467 / matchesReportExpectation :579。file:line はいずれも測定断面 = PR #3101 head `268f0d742` の packages/framework/core/tools/amadeus-election.ts — 以後の main 前進で行番号は移動しうる)。同種のドリフトが一度起きた面は 3 箇所以上の合意述語であることを疑う (learned 2026-08-15, intent 260815-priority-bug-batch-2 code-generation, full 梯子 AUTO_DECIDED auto-decision-137ee56e98679d723faefb160a8e4016) (learned 2026-08-15) <!-- cid:code-generation:cg2-agreeing-predicate-drift -->
- バグ修正の Red が既存テストの fixture 側でも赤くなる場合、その fixture がバグ挙動を契約として固定している可能性を先に疑う — fixture を「テストが通るように」直すのではなく、実条件を満たす形へ直して修正後の正しい挙動を証明する。実測: #3074 の recompose ガード修正で t246 の integration fixture が `Lifecycle Phase` フィールド自体を持たず常時 denied を assert していた(= バグ契約の固定)。fixture へ `- **Lifecycle Phase**: CONSTRUCTION` を足して実条件下の拒否を証明し直し、加えて IDEATION/INCEPTION では gated 実行と同一の拒否理由へ着地する behavioural equivalence を追加検証した(裸の否定 assert より強い) (learned 2026-08-15, intent 260815-priority-bug-batch-2 code-generation, full 梯子 AUTO_DECIDED auto-decision-137ee56e98679d723faefb160a8e4016) (learned 2026-08-15) <!-- cid:code-generation:cg2-fixture-pins-bug-contract -->
- Bolt PR の head checkout(bolt worktree)内で mint された record 生成物(pr-convergence report・audit シャード追記)は、conductor record への回収を Bolt 閉包の手順に含める — マージ後に landed を mint し直す前に、まず head checkout 側の未回収 mint を探す。lifecycle は converged→landed を拒否する(pre-merge に converged が成立していれば converged が最終形)ため、conductor 側の report が created のままでも「収束未成立」と誤読して landed 経路(削除済みリモートブランチの復元等)を準備するのは無駄手になる。実測: intent 260815-priority-bug-batch-2 の pr-convergence stage で、conductor record は kind: created(05:29)のまま、bolt worktree に kind: converged(2026-08-15T07:31:09Z mint、attestation prca:c8589eb0…、マージ 07:44:15Z の13分前)と付随 audit 行(amadeus.artifact.attested、shard 8c5251e2b98e seq 4)が未回収で残存。landed 再 mint は逐語『report lifecycle refused: converged -> landed』で拒否され、回収(cp + sort|uniq -d 重複行検査 0)が正しい閉包だった。既存 cid:code-generation:c2-pr-record-in-head-checkout(入力面: record は head checkout 内に実在)の補完 — 出力面: head checkout 内で mint された生成物は回収してから次段へ進む (learned 2026-08-16, intent 260815-priority-bug-batch-2 pr-convergence, full 梯子 AUTO_DECIDED auto-decision-54b36dc3d0c4e7d956b14541e0df7f28) (learned 2026-08-16) <!-- cid:pr-convergence:prc-headcheckout-output-sweep -->

- record checkpoint を同梱した Bolt PR は、他 intent の checkpoint 着地により `amadeus/spaces/default/intents/intents.json` で base 競合する(全 intent が同一 registry へ追記するため構造的)。解決はマーカー貼り合わせでなく「main 版全文 + 本 intent の entry を uuid 照合で追加」の再構成で行い、解決後に uuid 一意性の機械検査を必ず挟む — 解決直後でも hook の state 同期が旧断面の entry を再追記して同一 uuid の二重化を作ることを実測(dedupe は complete 優先)。複数 unit の record 同梱 PR は同 registry/record 面で相互にも競合するため直列着地(着地 → 次 PR を rebase → 再 mint → CI 再走)になる。実測: intent 260816-open-bug-batch-7 build-and-test(PR #3157 rebase、#3159 との競合、AUTO_DECIDED auto-decision-d88c3cafbe4619c581a775d707cf1abd) (learned 2026-08-16) <!-- cid:build-and-test:registry-merge-recomposition -->
- cid:code-generation:multiunit-pr-procedure への追補(直列着地の rebase 形): record 同梱の複数 unit PR は intents.json / record 面で相互競合するため、先行 unit の着地後の後続 unit は (1) 実装コミットだけを origin/main へ cherry-pick(旧 record bundle コミットは破棄) (2) conductor record を rsync(gitignored runtime を exclude)して bundle コミットを作り直し (3) push -f → create 再 mint → CI 再走 → converged → キュー投入、の反復で処理する。registry(intents.json)は再構成前に conductor/main の uuid 集合差を実測し、同一なら再構成不要。実測: intent 260816-open-bug-batch-7 の pi(#3161)/ sen(#3158)で 2 回反復し、いずれも競合ゼロで着地(learned 2026-08-16, full 梯子 AUTO_DECIDED auto-decision-82b3e971100271825091a2edbf4be217) (learned 2026-08-16) <!-- cid:pr-convergence:serial-landing-rebase-shape -->
- cid:pr-convergence:prc-landed-batch-serialize / c1-2 への追補(landed の適用境界): pr-convergence CLI の lifecycle 契約は converged を final state とし、converged→landed の書き換えを拒否する(`transitionAllowed` は created からのみ converged/override/landed へ遷移可 — `.claude/plugins/github-pr-convergence/tools/pr-convergence-cli.ts:610-617`)。landed 最終化は「report が created のままマージが先行した」stale 経路専用であり、マージ前に converged を確定させた unit に landed を再発行してはならない(拒否は正しい fail-closed)。実測: intent 260816-open-bug-batch-7 で nsd(#3157)への landed 発行が「report lifecycle refused: converged -> landed」で拒否され、stage 本文「Already merged?」節の適用条件(status が landed と答える場合のみ)と一致することを確認(learned 2026-08-16, full 梯子 AUTO_DECIDED auto-decision-82b3e971100271825091a2edbf4be217) (learned 2026-08-16) <!-- cid:pr-convergence:converged-final-no-landed-rewrite -->
- no-silent-drop の Evidence Reconcile workflow(push main 起点)は、adoption evidence の rebind コミットが当該 PR の**最終**コミットであることを要求する(そうでないと REBIND_BINDING_PR_TREE_MISMATCH で main が赤化する)。squash queue 下では rebind 後に record checkpoint 等を積むとこの条件が壊れる。修復形は「evidence 3 ファイルのみを diff とし、main tip へ rebind する単独 PR」。予防形は「rebind をブランチの最終コミットに保つ(rebind 後は push 前に追加コミットを積まない)」。実測: intent 260816-open-bug-batch-7 で nsd(#3157)着地後に Reconcile が赤化し、是正 PR #3162(→ aac8df6f2)で回復。pi / sen の着地(evidence pathspec 非接触)では Reconcile success を実測し再発なし(learned 2026-08-16, full 梯子 AUTO_DECIDED auto-decision-82b3e971100271825091a2edbf4be217) (learned 2026-08-16) <!-- cid:pr-convergence:reconcile-rebind-final-commit -->
- inception ステージの questions ファイルは起草時に depth の質問上限(Minimal = 4問、`## Qn:` 見出し単位で機械計数 — amadeus-sensor-question-budget.ts collectQuestionIds)へ収める。question-budget センサーはステージ実行時 advisory だが、record を Bolt PR へ同梱した瞬間に CI の t517 corpus sweep(blocking)が committed 全 questions ファイルへ発火する非対称がある。超過時の是正は関連質問の見出し統合(1見出し内の複数面は可 — [Answer] に各面の裁定 provenance を保持すれば選挙 definition の question 粒度と食い違ってよい)。実測: intent 260816-priority-bug-batch-3 で AD/UG/DP の各5問構成(測定 ref = record checkpoint 303b7ba8b、`git show 303b7ba8b:<record>/inception/<stage>/<stage>-questions.md | grep -c '^## Q'` → 5/5/5)が record 同梱済み Bolt PR の t517 corpus sweep を赤化(代表 run 31989387507)。見出し統合後の corpus は questions 4 ファイル(AD/UG/DP 各4問 + RA 2問、同 grep 述語を現 record 断面へ適用)となり、`bun test tests/integration/t517-question-budget-sensor.integration.test.ts` → 30 pass / 0 fail(tree = main 27f0d658b + 本 record)+ 各 Bolt PR の CI green で確認 (learned 2026-08-17, full 梯子 AUTO_DECIDED) (learned 2026-08-17) <!-- cid:code-generation:c1-question-budget-corpus -->
- builder subagent が作業中の bolt worktree で conductor が record sync コミットを作るときは、必ず `git add -- amadeus/ && git commit -- amadeus/` のパス限定で行う — worktree には builder の staged/unstaged 実装差分が存在しうるため、無限定の `git add`/`git commit` は builder の差分を chore(record) コミットへ無音で巻き込む。実測: 同 intent の B4 worktree で builder の BLOCKER 是正差分が conductor の record sync コミット 397fcd4ed に同梱された(内容は builder の diff 実読と対象テストの green で検証済み — 着地後の再導出: `bun test tests/unit/t206-source-work-intent-span.test.ts` → 21 pass / 0 fail、tree = origin/main 0b652d2cd 断面(record-only ブランチ checkout、tests/ は main と無差分を `git diff --stat origin/main -- tests/` で確認)。初回追記時の「14 pass、tree = main 27f0d658b」は測定 tree の申告誤り(実測定 tree は B4 変更未取込の conductor checkout)で、本行はその訂正。squash 着地でメッセージは消えるため履歴修正はせず)。並行 builder のいる worktree での conductor git 操作は全て対象パス明示を既定とする。なお pathspec 限定は実装差分の巻き込み防止であって同時書き込みの直列化ではない — `git commit -- <path>` はコミット時点の worktree 内容を対象にするため、builder が record 面(audit shard 等)へ書込み中だと部分状態を拾いうる。record sync コミットは builder のタスク完了・アイドルを実測した静止時点に限って行う (learned 2026-08-17, full 梯子 AUTO_DECIDED; 追補 2026-08-17 PR #3180 レビュー対応) (learned 2026-08-17) <!-- cid:code-generation:c2-record-sync-pathspec -->
- recompose の可動域は compose 提案時に織り込む — (a) intent-capture は birth した瞬間にカーソルが乗り in-progress となるため recompose で SKIP 不可(PENDING のみ反転可。SKIP したいなら birth 前に scope 側で落とすか、薄い実行で通す)。(b) self-feature では walking-skeleton ゲートアンカーが Construction 最初の EXECUTE ステージに固定され、そのステージ(例: functional-design)の SKIP 反転は拒否される — engine の正規代替は到達時 jump または scope 変更。composer が「ストックスコープ + recompose」経路の flips を提案するときはこの2制約を提案に反映する。実測: intent 260817-inception-cost-batch の birth 直後 recompose 2連拒否(エラー逐語「its checkbox is not pending ([in-progress])」「the walking-skeleton gate anchor」) (learned 2026-08-17) <!-- cid:intent-capture:c2 -->
- 新しい artifact kind の置き場を設計するときは、producing stage の path arm を先に実測する — resolveArtifactPath は owner stage で分岐し、KNOWN_CODEKB_STAGES(現状 reverse-engineering のみの単一要素集合)の produces は record ではなく共有 codekb へ解決するため、per-intent 証跡を RE の produces に載せると置き場が壊れる。per-intent 証跡は record arm を持つ stage(例: intent-capture)の optional_produces に宣言する — optional_produces は amadeus-stage-schema.ts:37 で実在し amadeus-graph.ts:860 の producersOf に含まれるため orphan hard error を満たし、SKIP スコープでも off-path producer は advisory に留まる。実測: intent 260817-inception-cost-batch application-design(ADR-1、断面 23d4ae767。§12a iteration-1 BLOCKER が「optional_produces 未検証」を突き、schema/graph の grep 実測で閉じた)(learned 2026-08-17, full 梯子 AUTO_DECIDED auto-decision-1b9427d447af96073fa6ecfa95cad595) (learned 2026-08-17) <!-- cid:application-design:c2 -->
- cid:build-and-test:bt-review-thread-gate-stale-fail への追補(coverage-flake の帰属手順): coverage 計測ジョブ(フルスイートを --coverage で再実行する Coverage Report (head))だけで落ちるテストは、まず**同一 run の Tests job での同テストの結果**と突合する — 同一 head・同一コードで Tests job が pass していれば coverage 実行下のみの flake と帰属でき、コード変更なしの `gh run rerun <run-id> --failed` が正しい回復になる(green ジョブは保持され失敗ジョブのみ再評価)。自変更の帰属切り分け(c1-ablation)の CI 内変種であり、ローカル再現や ablation を組む前にこの突合を先に引く。実測: PR #3190 run 32093105069 — t2967「spent済みprovenanceでも未回答のadvisoryが残ればrefusedになる」が Coverage Report (head) で fail、同 run の Tests job では pass、rerun --failed 後に全ジョブ green(learned 2026-08-18, intent 260817-inception-cost-batch code-generation, full 梯子 AUTO_DECIDED auto-decision-ba6623e928272fe686e4c3aa7e1ccf12) (learned 2026-08-18) <!-- cid:code-generation:user-1 -->
- cid:pr-convergence:prc-headcheckout-output-sweep への追補(還流 sweep の機械条件): worktree→conductor の record 還流で、対象ファイルの包含検査(`comm -23 <(sort conductor側) <(sort worktree側)`)が conductor-only 非ゼロを返したら **cp は禁止 — union 再構成(全行和集合+出現順保持の単調 seq 検証)だけが許される**。conductor 側は worktree 断面以後の追記(遷移行・attestation receipt)を持つのが常態であり、cp は未コミット追記を無音で喪失する。実測: intent 260817-inception-cost-batch の pr-convergence で conductor-only=94 を実測しながら cp を実行し未コミット 74 行を喪失(HEAD 版 748 行への復元で committed 分は無傷、bundle 断面 728 行が真部分集合であることは comm 0 行で実証)。喪失の顕在化は report-format sensor の「attestation event: canonical audit receipt is missing」(U1 の merged-arm 再 attest 行が喪失分に含まれていた)で、回復は merged-arm report の冪等再実行(receipt 再 mint)→ sensor PASSED。audit の捏造再現はしない(P2)(learned 2026-08-18, full 梯子 AUTO_DECIDED auto-decision-2c7c4861183ddd817706b90677226fa4)  (learned 2026-08-18) <!-- cid:pr-convergence:user-1 -->
- formal-model-check のローカル明示実行は `run-model-check.ts --model <tla> --cfg <cfg> --out <repo外>` の単一モデル経路を使う — `run-model-check-ci.ts run` の CI acceptance は runtime receipt に GitHub Actions env(RUNNER_OS/RUNNER_ARCH/GITHUB_RUN_ID 等)を要求するためローカルでは TLC 全 run 完走後でも構造的に ARTIFACT_VERIFY_FAILURE(exit 2)になる(実測: intent 260817-inception-cost-batch、24 run 完走後に「runtime receipt is incomplete」)。また同 CLI は --root 直下へ acceptance.json / verification.json / モデル別ディレクトリ / docker-*.argv 等の scratch を書き残す(#2555 の diagnostic 残渣と同族)— repo を --root にした場合は実行後に残渣除去まで行う。--out は scratchpad 等 repo 外を指定する (learned 2026-08-18, full 梯子 AUTO_DECIDED auto-decision-3410bb305f85af2472a133722268c8bf) (learned 2026-08-18) <!-- cid:formal-model-check:c2 -->
- cid:build-and-test:bt-ledger-resync への追補(第4の台帳クラス): ハーネス面の prose リテラルを toContain で pin するテストは、model-map 実装ハッシュピン / coverage-patch-allowlist 意味的セレクタ / coverage-registry と並ぶ resync 対象台帳である。7 conductor 面の文言を変える変更では census の対象集合に tests/ を必ず含める(述語例: `git grep -n -F -- "<退役リテラル>" -- packages/ tests/ docs/ .github/ plugins/ scripts/`)。同一リテラルについて存在を主張するテストと不在を主張するテストが対で存在しうるため、片方だけの更新は構造的な矛盾を作り必ず CI を赤にする — 実測では t425 が `--batch <n>` の存在を、t181 の HAND_TYPED_BATCH が同リテラルの不在を主張していた。実測: intent 260818-priority-bug-batch-4 で U1 の join 面 census(P1〜P7)が packages/framework/core/tools/ の code join のみを走査したため t425 が取り残され、run 32125609682 の Tests job 95675465924 と Coverage Report (head) job 95675465853 が 7 assertion で赤化(逐語 `Expected to contain: "prepare --batch <n> --units <all> --concurrency <directive.cap>"`)。是正は t425 の 7 リテラルを `<directive.batch>` へ resync(実装・prose は無変更)し、run 32135817142 で conclusion success (learned 2026-08-18) <!-- cid:build-and-test:bt-prose-literal-test-ledger -->
- 全数 census でファイル集合を走査するときは必ず配列で回す。このワークスペースのシェルは zsh でありクォートなし変数展開は語分割されないため、`FILES="a b c"; for f in $FILES` は 1 回だけ結合された不正パスで回り、grep は不一致を返して実在リテラルまで含む全件 0-hit の偽陰性 census になる。既知の ugrep 系トリガ(選言の複雑度超過・語境界 \b — cid:reverse-engineering:c6-absence-predicate-exit-code / c6-ugrep-word-boundary)とは別機序で、grep 自体は正常終了するため exit code 検査では捕まらない。健全性の判定手順: census には実在が既知の対照リテラルを 1 本以上含め、それが期待どおり全面一致することを確認してから 0-hit を不在の根拠にする。実測: intent 260818-priority-bug-batch-4 の build-and-test で、7 ハーネス面 × 16 リテラルの census が対照 `never owns queue order` を含めて全件 0/7 を返し、配列展開へ書き直して対照 7/7・退役形 0/7・新形 7/7 を得た (learned 2026-08-18) <!-- cid:build-and-test:bt-zsh-census-false-negative -->
- cid:code-generation:user-1 への追補(両方向への一般化): 単一ジョブだけで落ちるテストは、ローカル再現や ablation を組む前に同一 run・同一 head の別ジョブでの同テスト結果と突合して帰属する。既存則は Coverage 計測ジョブだけで落ちる場合を扱うが、逆方向(Tests ジョブだけで落ち Coverage ジョブでは pass)も同じ手順で帰属できる。回復はコード変更なしの `gh run rerun <run-id> --failed` または次 push による再走。実測: intent 260818-priority-bug-batch-4、PR #3202 run 32125609682 の Tests job 95675465924 で t433「a git ls-tree fault fails closed instead of emptying the trusted base set (#2576)」が fail、同 run の Coverage Report (head) job 95675465853 では同ファイルが pass(そちらの Failed files: 1 は t425 のみ)、ローカル b1 でも 14 pass / 0 fail。次 push の run 32135817142 では再発なし (learned 2026-08-18) <!-- cid:build-and-test:bt-cross-job-flake-attribution -->
- tla-authoring の適用性判定で impl-only を採るときの証拠形を 2 点セットに固定する — ステージ本文は『モデルが既に存在することを理由に impl-only を推論するな』と禁じるため、(1) 当該モデルの .tla に対する被検 subject 語彙の probe を**実在既知の対照リテラルつき**で走らせ、対照が非ゼロ・被検が 0 hit(exit 1 = 不一致であって exit 2 のエラーではない)であることを示す (2) model-map の vocabulary.namedInvariants を列挙し、被検 subject がそのモデルの守備範囲外であることを不変量名のレベルで示す。pinned implPath を触ったかどうかだけでは impl-only の根拠にならない(触っていなければそもそも non-target 側)。実測: intent 260818-priority-bug-batch-4 で amadeus-orchestrate.ts / amadeus-state.ts を 2 モデル(BoltPrAttestationGate / PrConvergenceGate)が pin する状況下、batch / pool / outcome / settle / cancel / supersed / directive / swarm が両モデルとも 0 hit、対照 VARIABLES / Init / Next が各 2 hit exit 0、namedInvariants にも swarm dispatch と outcome supersession が不在であることを示して terminal impl-only を確定(auto-decision-a2a5e5b2c5ff4d6282995cb993f05c69) (learned 2026-08-18) <!-- cid:tla-authoring:tla-impl-only-evidence-shape -->
- tla-authoring ステージ本文の選定基準(共有状態を持つ並行・再開可能アクター + 無音で残存しうる安全性違反)は team.md § Testing Posture の発火条件(並行プロトコルの spec 変更時のみ・全変更への一律義務化はしない)より広いため、選定基準に文言上該当しても author-new へ進まない場合がある。判別規則: 変更が**状態・遷移・相互排除規則を追加するか**で見る — fail-closed の拒否ガードの追加、および既存の別経路がすでに持つ契約への対称性回復は、いずれも新しい状態機械を導入しないため spec 変更に当たらず author-new を採らない。採らない判断は根拠つきで applicability-assessment へ明記する。実測: intent 260818-priority-bug-batch-4 の FR-2837-1(spent pool identity への再進入を emit 拒否で閉じる)と FR-3106-1/3(per-unit 経路へ pool 経路と同じ terminal outcome を回復。要件自身が『新しい第3の挙動を発明しない』と明示) (learned 2026-08-18) <!-- cid:tla-authoring:tla-spec-change-discriminator -->
- cid:pr-convergence:c1-2 への追補(再 mint に push が要る場合と要らない場合): stale created attestation の回復手順は『push the current HEAD, then run create again』だが、head が動いた原因が既に push 済みのコミットであれば push は不要で create 単独で新しい created epoch を開ける。判定は local HEAD == remote HEAD == PR head の 3 者一致と tracked worktree clean の実測で行い、一致していれば push を挟まない — 不要な push は CI を 1 周やり直させるうえ、その push でまた head が動く。実測: intent 260818-priority-bug-batch-4 の pr-convergence で、build-and-test の是正 push により両 PR の created attestation が stale 化(逐語『report attestation is stale: the PR head advanced to 7924e19146b0… since this report was attested at 010c3ca9c7e4…』)していたが、push なしの create 再実行で両 PR とも exit 0 で再 mint し、続く report が kind: converged を書いた (learned 2026-08-18) <!-- cid:pr-convergence:prc-create-remint-without-push -->
- converged report それ自体は自 PR へ同梱せず、後続 PR または final checkpoint で本線着地させる。自 PR へ commit すると head が動いて created epoch が再び stale 化し、create 再 mint → report → commit → stale の循環に入る。ステージ本文が『create → report は 1 つの head epoch 内で完結し、record checkpoint commit は verdict の後』と定めるのはこの理由。先行実績: 260817-inception-cost-batch の unit1 の pr-convergence-report.md(PR #3190 の converged)は Bolt 2 の PR #3191 と final checkpoint #3193 で本線着地している(`git log origin/main -- <report path>` で実測) (learned 2026-08-18) <!-- cid:pr-convergence:prc-report-lands-in-later-pr -->
- Bolt PR が運ぶ record の範囲は intent record(`amadeus/spaces/<space>/intents/<record>/`)+ `intents.json` + 変更した `specs/tla/model-map.json` に限る。codekb(reverse-engineering 成果)・elections ストア・memory(ノルム)は Bolt PR に含めず、final checkpoint または専用 PR で流す。実測: intent 260818-priority-bug-batch-4 の両ブランチで `git diff --name-only origin/main...HEAD -- amadeus/` を集計し、b1 = intents/<record> 32 ファイル + intents.json + specs/tla、b2 = 同 36 + intents.json + specs/tla で、codekb / elections / memory は 0 件 (learned 2026-08-18) <!-- cid:pr-convergence:prc-bolt-pr-record-scope -->
- formal-model-check の NOT_APPLICABLE 経路(直前の applicability outcome が impl-only / non-target / not-applicable で TLC を起動しない場合)では、ステージ本文 Step 4 の `plugin-activation.ts record .claude` を実行しない。同 verb の recordActivationVerdict は現行 spec hash を lastVerdictHash として永続化するため(`.claude/plugins/formal-model-check/tools/plugin-activation.ts` の recordActivationVerdict — 逐語コメント『persist the current spec hash as the last verdict hash』)、検査を一度も起動していないのに記録すると spec-change advisory を無音で抑止し『検証されたことにする』検証劇場になる。Step 4 の文言 After a completed check はこの発火条件を指す。実測: intent 260818-priority-bug-batch-4 の formal-model-check で、直前 route が terminal impl-only、engine も await-advisory-choice ではなく run-stage を emit(advisory は hold していない)、spec ファイル(.tla/.cfg)自体は本 intent 非接触だったため記録の必要も実益もなかった。代わりに model-completeness センサーを発火(passed)し、conductor ツリーの pin と実ファイル digest の一致を shasum で実測して drift 不在を示した (learned 2026-08-18) <!-- cid:formal-model-check:fmc-no-activation-record-on-not-applicable -->
- 版管理下にあり、かつツールが attest した単一状態を持つ成果物が、ツール契約の前進によって現行契約では不正な終端状態(他の kind へ遷移できない等)に固まっている場合、回復は手書き修正でもゲート迂回でもなく、git 履歴に残る当該ツール自身の生成 bytes を byte-exact で復元し、現行ツールを再実行して再 attest させる。復元の前提(復元元断面が当該ツールの生成物であること、祖先関係、監査受領行の現存)を実測し、根拠(破損 commit・復元元 commit・受領行 ID・実測コマンド)を record へ残す。適用面の明示: これは同一成果物が過去に自分自身として生成した bytes の原状復元(回復操作)であって、`cid:build-and-test:c1-ablation-before-artifact-repro` が禁じる帰属切り分け時のツリー間 byte-copy(自変更由来の欠陥を masking する行為)ではない。また `cid:deployment-pipeline:c3`(append-only 生成物の誤りは git revert で回復)とは適用面が異なる — 本則の対象は append-only でない単一状態の成果物であり、破損書込が他 record を含む checkpoint commit へ同梱されている場合 revert は過剰破壊になる。予防側の `cid:pr-convergence:converged-final-no-landed-rewrite`(final verdict へ landed を再発行しない)に対し、本則は既発生の破損からの回復側のみを扱う。実測: intent 260815-rfc-autonomy-modes の unit config-visibility — 破損 commit 7b8c552a0(2026-08-16T13:06:42Z)が CLI 生成済み converged 報告(attestation prca:4613a9a2…、収束台帳 resolved:6 / terminalized:5)を kind: landed へ書き換え、landed は code-generation スコープの blocking sensor が必ず不合格にする kind(plugins/github-pr-convergence/tools/amadeus-sensor-pr-convergence-report-format.ts:470-475)かつ遷移不能(同 pr-convergence-cli.ts:639-645)のため code-generation ゲートが構造的に閉じられなくなった。d00103a64 の CLI 生成 bytes を byte-exact 復元(git diff 差分 0 を実測)し、現行 CLI の report merged arm で再最終化 → 新 attestation prca:06e5b0d13…、code-generation / pr-convergence 両スコープで SENSOR_PASSED を実測。残存欠陥は Issue #3235 として起票済み。選挙 E-260819-RFC0001-CG-S13 で 2-0 established(GoA 2x2)、両票の留保(一般形で書く・適用面の明示スコープ・既存3則との境界)を本文へ反映済み (learned 2026-08-19) <!-- cid:code-generation:c1-tool-contract-restore -->
- 手動の `bun .claude/tools/amadeus-sensor.ts fire` の対象集合は、run-stage directive の `produces` / `optional_produces` から導出する — ステージディレクトリの列挙(`for f in <stage-dir>/*.md`)から作らない。理由は2つ。(a) 自動 hook 経路は document-shape / governance カテゴリのセンサーに invocation scope 検査を課すが(`packages/framework/core/tools/amadeus-sensor-invocation.ts:110-118` `sensorAllowsInvocationOutput` → `:74-87` `invocationDeclaresOutput` が produces / optional_produces へ絞る)、conductor の手動 fire 経路は設計上ゲートされない(同ファイル :22-23 逐語「the conductor's manual `amadeus-sensor.ts fire` path is not gated and remains the authoritative gate-preparation check」)。センサーの `matches` glob は produces 外のファイル(stage 日誌 memory.md 等)を除外しないため、ディレクトリ列挙は容易に対象外の面へ発火する。(b) 監査は append-only であり、誤発火が生んだ SENSOR_FAILED 行は取り消せない — 実在しない finding が恒久的な証拠として残り P2(記録は実測事実のみ)に反する。実測: intent 260815-rfc-autonomy-modes の build-and-test で、produces 7 件に含まれない `construction/build-and-test/memory.md` へ upstream-coverage を手動発火し、audit shard `j5ik2o-mac-studio-lan-d2b1764fdb4f.jsonl` の seq 149 SENSOR_FIRED / seq 150 SENSOR_FAILED(fire id a4fba979、2026-08-19T08:07:24Z)が終端として残存した。他 7 成果物は上流入力節の追記後に再発火して PASSED へ更新できたが、memory.md は produces でないため更新すべき面がない。近接則 `cid:formal-model-check:fmc-no-activation-record-on-not-applicable`(発火条件外で record verb を実行しない)と同族であり、次回蒸留での統合候補とする。手動 fire 経路そのものへの produces 検査追加(機械化)は Issue 起票が望ましく、本ノルム追記はその代替ではない。選挙 E-260819-RFC0001-BT-S13 で 2-0 established(GoA 2x2)、両票の留保(機械判定可能な一般則として書く・file:line と audit seq の一次証跡併記・近接則の相互参照・機械化は別途 Issue)を本文へ反映済み (learned 2026-08-19) <!-- cid:build-and-test:c3-manual-sensor-fire-scope -->
- 選挙の tally が established にするのは **choice** であって、各票の rationale に含まれる事実主張ではない。2-0 で確定した選挙の根拠であっても、その中の事実主張は行動(起票・実装・修正)の前に conductor が一次資料を実読して検証する。適用範囲は**内部由来の事実主張** — 選挙票の rationale / reservation、サブエージェントの完了報告 — であり、既存の `cid:pr-convergence:c1` が untrusted data 扱いを課しているのは外部レビュー本文(CodeRabbit)のみで、「内部の 2-0 verdict」という権威に見える出所は未被覆であるためこの上乗せが要る。判別式: 起票・修正の可否は主張者の人数や票差ではなく、**再現手順を構成できるか、または一次資料から直接検証できる証拠(スキーマ違反・不変条件違反・到達不能な遷移など)を提示できるか** で決める。実行時の再現手順がないことだけを理由に、一次資料から直接証明できる欠陥を対象外にしない。とくに、コードのコメントが危険を説明している場合、それは**ガードの存在理由**であって欠陥の証拠ではない — この誤読パターンを名指しで警戒する。再現手順も直接検証可能な証拠も欠く主張を起票・修正の根拠にすれば team.md P2(記録と検証は実測事実のみ・推測起票の禁止)に反する。クロスリファレンス: team.md P2 / `cid:requirements-analysis:mechanism-cite-verify-at-draft`(引用は起草時に実測)/ `cid:pr-convergence:c1`(外部レビュー本文の untrusted data 扱い — 本則はその適用面を内部由来へ拡張する)。実測: intent 260815-rfc-autonomy-modes の tla-authoring で、選挙 E-260819-RFC0001-TLA-ROUTE の両票が author-new の根拠に「waiting record を park 経路で resume すると never-parked な record に WORKFLOW_UNPARKED が emit され台帳不整合になる」を live な安全性違反として挙げた。起票前の実読で、`packages/framework/core/tools/amadeus-waiting.ts:288-307` の `resumeInterruption` が三終端を相互排他に dispatch し未知 stop reason を `notSuspendable` で拒否すること、および `packages/framework/core/tools/amadeus-state.ts:1641-1649` の `unparkLocked` が `wasParked` が真のときだけ `WORKFLOW_UNPARKED` を emit することを確認し、当該記述はガードの存在理由を説明するコメントであって live 欠陥ではないと判定した。再現手順を構成できず、かつ一次資料は当該欠陥ではなくガードの存在を示していた(直接検証できる証拠も不在だった)ため起票を差し戻し、ユーザー裁定で起票指示は撤回された。本学習は §13 選挙 E-260819-RFC0001-TLA-S13 が tie(1-1)で hold となり、team.md 正準リスト (1)「選挙の可否同数」としてユーザーが裁定した(選択肢 A、2026-08-19)。両票の留保(一般形で書く・内部由来への適用範囲明示・誤読パターンの名指し・既存則への cid クロスリファレンス)を本文へ反映済み (learned 2026-08-19) <!-- cid:tla-authoring:c1-internal-claims-are-untrusted -->
- tla-authoring が `author-new` へルートしたのち、ステージ本文 Steps 5 の human gate でユーザーがモデル作成(Steps 2〜6)を別 intent へ分離する裁定をした場合、後続の formal-model-check は新規登録モデルを持たない状態で走る。このとき本文 `:37` の逐語 `A missing or contradictory outcome is a halt.` の免除は、次の3条件を**すべて**満たす場合に限る(fail-closed)。(a) 分離の provenance — ユーザー裁定の日付と分離先 Issue 番号 — を applicability-assessment へ明記する。(b) 既存の登録モデルに対する検査結果は **drift 不在と既存不変量の無退行の証拠に限定**し、「分離した subject は未検証である」ことを open item として書き分ける。無関係な検査の成立を対象 subject の検証成立へ代入しない(`cid:build-and-test:verdict-names-unverified-facets`)。(c) `cid:build-and-test:no-silent-scope-narrowing` との境界を明示する — 本免除は「要件・RAID が『実装時に実測確認』と規定した項目の先送り」ではなく、ステージ自身が持つ human gate におけるスコープ確定にのみ適用される。3条件のいずれかを欠く分離は halt であり、「分離すれば halt を回避できる」という一般則としては読まない。実測: intent 260815-rfc-autonomy-modes — 選挙 E-260819-RFC0001-TLA-ROUTE が author-new を 2-0 で確定させたのち、human gate でユーザーが分離を裁定(2026-08-19、分離先 Issue #3246)。formal-model-check では登録 4 モデル(BoltPrAttestationGate / FormalElection / MirrorLifecycle / PrConvergenceGate)が NOT_DETECTED(exit 0)、model-map の実装ハッシュピン 13 エントリ全て MATCH(drift 0)、model-completeness センサー exit 0 だったが、applicability-assessment の語彙 census と `vocabulary.namedInvariants` 全列挙により、これら 4 モデルは選定 subject の FR-3(waiting terminal)を一切覆っていないことが確定しているため、FR-3 は未検証の open item として持ち越した。conductor は当初これを「本ステージの検証実績」と記録したが、§13 選挙の両票が独立に検証劇場と指摘し、実読で確認して撤回・訂正した。選挙 E-260819-RFC0001-FMC-S13 は tie(1-1、subagent-1 が choice 3 = 0 件、subagent-2 が choice 1)となり、team.md 正準リスト (1) としてユーザーが裁定(選択肢 A、2026-08-19)。両票の収束点(候補2の不採用)と subagent-2 の3条件を本文へ反映済み (learned 2026-08-19) <!-- cid:formal-model-check:c1-authornew-separation-halt-exemption -->
- cid:code-generation:falling-proof-injection-one-set / cid:code-generation:tdd-default-with-narrow-exceptions への追補(FD 文書面の特殊化): 落ちる実証を含む functional-design 成果物では、**配送順の手順番号列と TDD Red 先行の時系列を明示的に区別**して書く — 番号順どおりに実装するとテストより先に対象(glob・コード)が更新され Red が観測不能になる自己矛盾クラス(実測: intent 260820-fmc-drift-batch の boundary-three-face FD §12a iteration 1 BLOCKER — 手順3 glob 更新が手順4 drift テスト新設に先行し「現行断面で赤」と矛盾 → テスト先行へ再番号で解消)。また **per-unit 成果物は他 unit の着地順を「着地済み」の前提事実として書かない**(承認済み unit graph にない着地順依存は P3 の無申告依存クラス)— 必要なら落ちる実証を述語レベル(他 unit 非依存の実 corpus 直接入力)と pipeline レベル(fixture)の二層へ分解する。是正時は同種文言を**表セル含む文書内全表現の総なめ**で揃える — 散文だけ直して表セルに残存し §12a iteration を1回消費した実測(同 intent applicability-arms iteration 1→2、残 BLOCKER = domain-entities.md 表2箇所)。選挙 E-260820-FMC-FD-S13 は tie(1-1)hold → 正準リスト(1)ユーザー裁定(2026-08-21、L-A のみ採用。L-B = iteration 上限後の quality-repair 経路の規範化は「未検証の先取り記述」として不採用 — subagent-2 の反証を採る)。subagent-1 の留保(既存 cid への相互参照必須・特殊化である旨の明記)を本文に反映 (learned 2026-08-21) (learned 2026-08-20) <!-- cid:functional-design:c4 -->
- swarm invoke 時の配送形(merge-queue 必須プロジェクトでの確定): swarm finalize のローカル merge 段は、`amadeus-worktree.ts` handleMerge(:369-415 — main checkout の HEAD==--target 要求 + ローカル本線 squash/merge/rebase)と finalize の target 既定(prepared base ?? main、amadeus-swarm.ts ~:1210)により、本プロジェクトの確定済み配送規範(FR-X-2: Bolt PR + merge queue、cid:ci-pipeline:strict-up-to-date-before-merge の必須 Merge Queue Ruleset)と構造的に衝突する — 使わない。referee は pool 管理 verb(resolve / prepare / acquire / confirm-dispatch / check / settle-release)までを正とし、配送は per-unit Bolt PR(record bundle → create → converge → 常任承認条件で queue)で行う。engine は finalize なしでも pool terminal で次 batch を発行する(実測: intent 260820-fmc-drift-batch batch 1→2)。SKILL の finalize 契約からの乖離は毎回 diary へ記録する。**暫定運用**: 両票の留保どおり framework Issue(swarm finalize と merge-queue 前提配送の非互換 — PR 配送 / no-local-merge モードの追加)を起票し、その着地までの運用とする。次回蒸留で起票状況を確認。選挙 E-260820-FMC-CG-S13 2-0 (learned 2026-08-21) (learned 2026-08-21) <!-- cid:code-generation:c1-swarm-pr-delivery -->
- cid:code-generation:c1-question-budget-corpus 等の t146 系追補(第6の衛生クラス): plugin の .md prose(sensor manifest の `matches` glob 分岐を含む)内の repo-root 相対 `plugins/<kebab>/(tools|stages|specs|hooks)/` パスリテラルは t146(ROOT_RELATIVE_PLUGIN_PATH_RE、tests/unit/t146-core-hygiene.test.ts:89-90)が拒否する — 先頭・`./`・`../`・非パス文字直前の全てが対象で、`,` 区切りの brace glob 分岐も発火する。glob では `*/plugins/...` の anchored 形(直前が裸 `/` になり regex 3 分岐のいずれにも非該当)が意味論を保存して回避できる — 採用前に本番 `matchesGlob`(amadeus-sensor.ts:1121)へ絶対パス標本を与えて match 集合の不変を実測する。実測: intent 260820-fmc-drift-batch PR #3364 の CI 1周目赤 → anchored 形 cb6c88c09 で緑。選挙 E-260820-FMC-CG-S13 2-0 (learned 2026-08-21) (learned 2026-08-21) <!-- cid:code-generation:c2-t146-anchored-glob -->
## Review Thread Gate の stale fail は rerun --failed で回復する
- CI Review Thread Gate はレビュースレッド resolve 前に評価されると stale fail し、resolve イベント起点の Refresh review-thread workflow は連続 resolve の concurrency cancel で全滅しうる(4 スレッドを数秒間隔で resolve した場合に全 run cancelled を実測)。回復は対象 run の完走後に `gh run rerun <run-id> --failed` — gate と集約 CI Success だけが再評価され、他ジョブの green は保持される。実測: PR #3113 run 31890284881(初回 failure → rerun 後 conclusion success)(learned 2026-08-15, intent 260815-stale-epoch-landed build-and-test, full 梯子 AUTO_DECIDED auto-decision-62835c2cb1937bc1c9968c849b9df247) (learned 2026-08-15) <!-- cid:build-and-test:bt-review-thread-gate-stale-fail -->

## 複数 unit の landed 最終化は unit ごとに report → record commit を直列で挟む
- landed 最終化(pr-convergence CLI report の merged arm)の foreign-dirty 検査は、self-output(当該 unit の report + record の audit shard)以外の tracked 変更を fail-closed で拒否する。先行 unit の report 更新は次 unit にとって foreign なので、複数 unit を一括実行すると 2 unit 目以降が「tracked worktree is dirty」で全滅する — unit ごとに report → `git add -u` + record checkpoint commit を挟んで直列化する。実測: obb6 5 unit の resume 断面(2026-08-15)で一括 1 回目に 4 件拒否 → 直列化で 5/5 landed(各 sensor 再 fire exit 0)(learned 2026-08-15, intent 260814-open-bug-batch-6 pr-convergence, full 梯子 AUTO_DECIDED auto-decision-4e54d5ea239bafc33b006a2ec602366d) (learned 2026-08-15) <!-- cid:pr-convergence:prc-landed-batch-serialize -->
- multi-unit intent(units-generation / delivery-planning EXECUTE)の per-unit PR 作成は次の定型を踏む — (1) delivery-planning の bolt-plan.md は `## Bolt <n>` 見出し + `- **Units:** `バッククォート付き slug の機械可読形式が必須(`amadeus-delivery-bolts.ts` parseDeliveryBoltSection の parser 契約。散文だけだと runtime compile が「must contain at least one valid Unit slug」で拒否) (2) bolt worktree へ `amadeus/active-space` と `active-intent` カーソル(gitignored・per-clone)を複製したうえで worktree 側の `amadeus-runtime compile` を実行し Delivery Bolt authority projection を生成(不在だと create が DELIVERY_BOLT_AUTHORITY_MISSING) (3) record checkpoint を bolt ブランチへ同梱 commit してから `pr-convergence create`(--bolt は Bolt 見出し由来の番号。slug 指定は 0 件 MISMATCH) (4) create が mint した report と audit shard を conductor record へ還流してから §12a を回す — reviewer-runtime scope は per-unit produces の pr-convergence-report.md 実在を要求するため、PR 作成がレビューの前提になる。実測: intent 260816-open-bug-batch-7 code-generation(PR #3157/#3158/#3161、AUTO_DECIDED auto-decision-0972a2aedeeff615beaba1bd2d75ee1c) (learned 2026-08-16) <!-- cid:code-generation:multiunit-pr-procedure -->

## issue-first の self-fix intent では RE 前に issue-evidence fetch を手動実行する

- self-fix スコープは intent-capture を SKIP するため、#3181 の issue-evidence.md(クロスレビュー証跡の上流入力)が自動では record に作られない。issue-first の self-fix intent では reverse-engineering の実行前に、orchestrate ループ外の read-only verb `bun .claude/tools/amadeus-utility.ts issue-evidence fetch --issues <n[,n...]>` を conductor が手動実行して `<record>/ideation/intent-capture/issue-evidence.md` を成立させる(consumes 宣言は追加しない — RE は body-level 読取、RA は required:false 消費)。実測: intent 260818-priority-bug-batch-4 で初回適用、fetch 成功後に RE の focus 導出が issue-evidence 経由で成立 (learned 2026-08-18, ユーザー採用裁定) (learned 2026-08-18) <!-- cid:reverse-engineering:c1-selffix-evidence -->
