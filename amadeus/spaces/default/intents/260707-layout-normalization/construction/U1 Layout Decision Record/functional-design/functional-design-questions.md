# Functional Design Questions — U1 Layout Decision Record

## 質問生成の判断

追加質問は生成しない。`unit-of-work`, `unit-of-work-story-map`, `requirements`, `components`, `component-methods`, `services` により、U1 の成果物境界は定義済みである。

## 実装前確認

Code Generation では、ADR または design record の最終配置 path を repository convention に合わせて確認する。候補は `docs/adr/`, `docs/reference/`, issue-linked design artifact である。
