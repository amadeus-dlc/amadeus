# Functional Design Questions — claude-tui-live

参照入力: `unit-of-work`、`unit-of-work-story-map`、`requirements`、`components`、`component-methods`、`services`。

## 既決照合

Issue #1717と上流成果物が専用opt-in、暗黙設定廃止、private tmux、project settings、cleanup、greenまたはevidence Issueを定義済みである。矛盾・欠落はなく、`Guide me`継続、追加質問0件。

## Plan

3つのFunctional Design成果物を生成する。library Unitのためfrontend成果物は生成しない。
