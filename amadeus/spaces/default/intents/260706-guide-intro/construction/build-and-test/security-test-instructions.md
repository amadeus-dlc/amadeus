# Security Test Instructions

Unit: guide-intro（Test Strategy: Minimal）

## 適用判断

不適用とする。

## 根拠

本 Intent は公開リポジトリ内文書の新設のみで、認証情報・入力境界・実行コードに触れない。掲載した実測出力にも秘密情報は含まれない（隔離 workspace の一時パスは <workspace> へ置換済み）。Testing Posture（project.md）の規約に従い、不適用の instruction は空ファイルにせず本判断を記録する。
