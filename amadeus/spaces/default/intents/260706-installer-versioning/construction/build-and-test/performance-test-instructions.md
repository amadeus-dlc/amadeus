# Performance Test Instructions

Unit: u001-installer-versioning（feature scope）

## 適用判断

性能テストは適用しない（PERF-1 = SLO なし、[performance-requirements.md](../u001-installer-versioning/nfr-requirements/performance-requirements.md)）。eval 実行時間が CI の実用範囲であることは test:all の運用で観測する（#451 と同じ扱い）。
