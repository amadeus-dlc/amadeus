# Feasibility & Constraints — 質問と回答

- **Intent**: 260708-installer-distribution
- **ステージ**: feasibility (1.3)
- **モード**: Grill me(グリリング — 質問は動的に1問ずつ追記される)
- **深度**: Standard(前 intent の合意(6問)の再確認+新規事実照会の反映)

> このファイルは意思決定の正式記録。グリリングモードでは事前一括作成をせず、提示直前に各質問を追記し、回答受領後ただちに `[Answer]:` タグへ書き戻す。

---

## Q1. npm 公開前提の現況確認とパッケージ名の確定

本日(2026-07-08)npm レジストリを照会した結果(事実):

- `@amadeus-dlc/setup` → **404(未公開・名前は空いている)**
- `@amadeus-dlc/framework` → **404(未公開)**
- 非スコープ `amadeus-dlc` → 404
- npmjs.com の org ページは bot 保護(403)で確認不能 — **`amadeus-dlc` スコープ(org/ユーザー名)を第三者が保有していないかはレジストリ照会だけでは確定できない**

推定(確信度: 中): スコープ配下にパッケージが1つも存在しないため、スコープは未取得か、取得済みでも未使用。公開前タスクとして「npm org `amadeus-dlc` の作成(またはスコープ確保)」を意思決定者(メンテナ)が実施する必要がある — 前回 RAID の R1 を維持。

パッケージ名 `@amadeus-dlc/setup` を維持し、スコープ確保を公開前タスク(RAID: R1)として登録するか?

- A. 維持 — `@amadeus-dlc/setup` + R1 登録(推奨: GitHub org 名と一致しブランド一貫性がある。名前は現時点で空いており、確保は数分の作業)
- B. 変更 — 別名にする(候補を指定)
- X. Other(自由記述)

[Answer]: A. 維持 — `@amadeus-dlc/setup` を維持し、スコープ確保を公開前タスク R1 として登録(2026-07-08、Mode: grilling)

---

## Q2. 配布アーキテクチャの再確認

前回合意(feasibility 6問)の配布アーキテクチャ:

1. **インストーラ**: npm 公開 CLI(`@amadeus-dlc/setup`)。TypeScript をビルド(JS 化、単一ファイルバンドルが現実的)して **npx(Node)/ bunx(bun)両対応**
2. **配布物(フレームワーク本体)**: npm パッケージに同梱せず、**GitHub からタグ指定アーカイブ取得**(公開リポジトリ・認証不要)。インストール時にネットワーク到達必須
3. **バージョン整合**: インストーラ(npm)と配布物(GitHub タグ)が別経路のため、対応規約を設計で明確化(RAID: R3)

この配布アーキテクチャを維持するか?

- A. 維持 — npm CLI + GitHub タグ取得の2経路構成をそのまま採用する(推奨: 配布物を npm に同梱するとフレームワーク版とインストーラ版が結合しリリース柔軟性を失う。タグ取得は既存のバージョンタグ運用と自然に整合)
- B. 変更 — 配布物も npm パッケージに同梱する(単一経路化)
- C. 再検討 — 別の配布構成を議論したい
- X. Other(自由記述)

[Answer]: A. 維持 — npm CLI + GitHub タグ取得の2経路構成をそのまま採用(2026-07-08、Mode: grilling)

---

## Q3. 運用制約とライセンス是正の再確認

前回合意の運用・コンプライアンス制約:

1. **リリース運用**: npm publish は既存のバージョンタグ運用に統合し、手順を明文化(独立した publish CI 自動化は初回スコープ外)(O1)
2. **タイムライン**: 制約なし、品質優先(O2)
3. **ライセンス是正**: package.json の `"license": "MIT-0"` は LICENSE-MIT + LICENSE-APACHE のデュアル構成と矛盾 — **本日確認したところ未修正のまま残存**。公開前の是正が必須(C1/I1)
4. **AWS/規制**: クラウドインフラ不要、PCI/HIPAA/SOC2 該当なし、テレメトリなし

この運用制約セットを維持するか?

- A. 維持 — 4点をそのまま採用する(推奨: 前回から状況変化はなく、ライセンス是正の必須性は本日の実測で再確認済み)
- B. 修正 — 特定の制約を変更する(内容を指定)
- X. Other(自由記述)

[Answer]: A. 維持 — 運用・コンプライアンス制約4点をそのまま採用(2026-07-08、Mode: grilling)
