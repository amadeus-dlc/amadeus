# Discovered Rules — practices-discovery(260712-metrics-observation)

新規に発見・昇格すべきルール: **なし**(evidence.md のギャップ検査どおり)。

将来候補(本 intent の実装着地後に §13/PM で判断 — 先行 affirm しない):
- metrics-as-code の運用則(snapshot の読み方・不連続点の扱い)— 実績形成後
- snapshot 用 workflow の権限最小化パターン(release.yml 前例の一般化)— 2例目が出た時点
