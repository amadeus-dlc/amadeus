# Domain Entities — U1

- `TestsTotals`: { files: number, failedFiles: number, assertions: number, failedAssertions: number } — 判別不要の値オブジェクト(brand 不要: 取り違え対象が存在しない単一使用点。ddd-when-to-wrap-primitives の「包まない」側)。
