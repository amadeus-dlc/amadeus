# 根拠バグ射程台帳 — 直接根拠9件(FR-6a)

本台帳は Issue #1980 の直接根拠9件について、本 intent(260802-record-roundtrip-pbt)の射程判定と、射程外の分担先を固定する記録である。判定は既存出典からの**転記のみ**で、本台帳では再判定しない(BR-SL-8)。

## 出典と取得メタ(三点併記 — R-1〜R-3)

- 取得コマンド(実行形そのまま): `gh issue view 1980 --json title,body --jq '.body'`
- 取得日時(UTC): 2026-08-03T02:15:17Z / 取得本文の総行数: 75 行
- 射程判定の正本(1次) — 取得本文 `:71` verbatim: `- 根拠バグ: 上記9件（射程内: #1547 #1459 / 部分: #1871 #1946 / 射程外→分担: #1904 #1878 #1953 #1860 #1906）`
- 射程外の分担先(2次) — 取得本文 `:64` verbatim: `#1878（戻り値破棄=無音化）→ #1979 no-silent-drop 静的ゲート / #1860（状態機械の到達可能性）→ #1981 形式検証 CI / #1906（並行性 → 主に #1981。テスト側の無音成功検出という面では #1982 とも同族）/ #1953（意味論的鮮度）→ 個別修正`
- 1行要約の正本 — 取得本文 `## 背景・実測` の箇条9行(本取得断面では `:15-23`)
- 対象9件と行順の正本 — `requirements.md:47` verbatim: `直接根拠9件(#1904 #1878 #1946 #1953 #1906 #1860 #1459 #1547 #1871)`

行番号についての注記: 上流 FD は同じ箇所を `:72` / `:65` / `:16-24` と引く。本取得断面では一律 1 行だけ前にずれる(`:71` / `:64` / `:15-23`)。R-1 に従い locator 単独に依存せず verbatim 断片を併記しているため、いずれの断面でも全文検索で再解決できる。内容の同一性は上記 verbatim で確認済みであり、判定写像に変更はない。

## 射程台帳(9件)

| # Issue | 1行要約(出典からの転記) | 射程判定 | 分担先 | 根拠 |
| --- | --- | --- | --- | --- |
| #1904 | standing grant の発行⇔消費非対称、ソロで無音不適用 | 射程外 | 未割当(出典に記載なし) | 判定=`:71` / 要約=`:15` / 除外理由=`:50` |
| #1878 | persistBlocked の喪失無音化(#1860 同根の残存) | 射程外 | #1979 | 判定=`:71` / 要約=`:16` / 分担=`:64` |
| #1946 | ballot submittedAt が自己申告・無検証で台帳と矛盾 | 部分 | — | 判定=`:71` / 要約=`:17` |
| #1953 | approve 側突合が stale 実績を受理 | 射程外 | 個別修正 | 判定=`:71` / 要約=`:18` / 分担=`:64` |
| #1906 | state lock 競合で増分が無音消失 | 射程外 | #1981(主)/ #1982(同族) | 判定=`:71` / 要約=`:19` / 分担=`:64` |
| #1860 | receipt prepared 残りで complete 不能 | 射程外 | #1981 | 判定=`:71` / 要約=`:20` / 分担=`:64` |
| #1459 | Election.parse が重複 internalNo/重複 voter/空 choices を無音受理し tally 汚染 | 射程内 | — | 判定=`:71` / 要約=`:21` |
| #1547 | 状態表現の write⇔read 非対称で mirror-missing 偽陰 | 射程内 | — | 判定=`:71` / 要約=`:22` |
| #1871 | mirror title 無クランプで GitHub 422 | 部分 | — | 判定=`:71` / 要約=`:23` |

件数の機械再計算: 射程内 2(#1459 #1547)+ 部分 2(#1946 #1871)+ 射程外 5(#1904 #1878 #1953 #1906 #1860)= **9**。出典 `:71` の3群の要素数の和と一致する。

## 注記

- **#1904 の分担先は未割当**である。取得本文 `:64` と `requirements.md:80` の射程外分担列挙はいずれも #1878 / #1860 / #1906 / #1953 の **4件**で、#1904 を含まない。`:50` は verbatim `**#1904 は round-trip の射程外（env 述語の非対称・直列化を伴わない）につき候補から除外**` と射程外に置くのみで行き先を定めていない。推測で Issue 番号を補わない(BR-SL-5)。
- 射程内・部分の4件の分担先が `—` なのは、本 intent 自身が担うためである(BR-SL-6)。出典 `:71` は射程内・部分に分担先を与えていない。
- 出典間に相違はない。`:71`(1次)と `:64` / `requirements.md:80`(2次、射程外4件の分担先のみ)は矛盾せず、後者は前者の部分集合に対する補足である(BR-SL-8)。
- 本台帳は分類記録であり、修正方針・設計判断・実装計画を含まない(BR-SL-11)。射程内2件の実装契約は `requirements.md` FR-1 / FR-4d と `decisions.md` ADR-4 が正本である。
- 44件全量の分類台帳化は本台帳の責務ではなく #1979 が担う(`requirements.md:47` / `:81`)。

## 受入検査(business-logic-model.md §5 の A1〜A5)

| ID | 不変量 | 結果 |
| --- | --- | --- |
| A1 | 指定パスに文書が実在する | PASS |
| A2 | 9件の Issue 番号がすべて記載され、異なり数が 9 | PASS |
| A3 | 各件に射程判定が1行あり、判定語彙が3値のいずれか | PASS |
| A4 | 対象外の Issue を足していない(表の行数 = 9) | PASS |
| A5 | 判定が出典(§4 写像表)と全9行一致する | PASS |

検査は R-6 に従い1つずつ独立ステップで実行し、exit code を個別に読んだ(`&&` 連鎖の中間に `grep -c` を置かない)。あわせて S-5 の秘密様パターン(パターン集合は nfr-design/security-design.md §3 が正本)の grep が 0 件であることを実行して確認した。台帳本文にパターン文字列そのものを書かない — 自己参照ヒットで検査が空文化するため。
