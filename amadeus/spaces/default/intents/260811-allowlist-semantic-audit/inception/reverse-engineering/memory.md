<!-- INVARIANT: examples are single-line HTML comments so a fresh template parses to total=0 (MEMORY_EMPTY). Do NOT un-comment or split across lines. t100 guards this. -->
> This file is maintained by the orchestrator during stage execution. Add observations at the gate ritual, not by editing here directly.

## Interpretations
<!-- example: 2026-05-29T10:14:32Z — chose REST over GraphQL; the consuming team only needs CRUD, revisit if subscriptions land -->
- 2026-08-11T13:37:00Z — 差分リフレッシュの base を `ce3c3ccfd`(直前 intent `260810-test-time-factor` の observed)に確定; `git merge-base --is-ancestor` exit 0、`git rev-list --count ce3c3ccfd..HEAD` = 34 で祖先かつ最小距離(`cid:reverse-engineering:rescan-base-ancestry`)。observed は worktree HEAD `854692fd7` = origin/main 系譜(`cid:reverse-engineering:c2-observed-mainline-commit`)
- 2026-08-11T13:37:30Z — **xrev differential scan mode は採らない**; #1622 のクロスレビュー2名 CONFIRMED は 2026-07-28 付で、その後 PR #2127 が allowlist を行ピンから意味的セレクタへ全面移行しているため、レビュー断面と observed の間で患部の表現形式そのものが変わっている。`cid:reverse-engineering:c1-xrev-single-issue` の成立条件「verdict が検証 SHA と file:line を明記」は満たすが、`cid:reverse-engineering:E-XBB-RE-S13-c2` の currency 条件(review..observed の実 diff が被引用パスと交わらない)が構造的に不成立。よって通常の差分スキャンとし、Issue コメントの実測例は observed 断面で取り直す

## Deviations
<!-- example: 2026-05-29T10:14:32Z — skipped the optional caching layer the stage prose suggested; the dataset is small enough that it adds risk -->
- 2026-08-11T13:38:00Z — Step 2 の scan を explore 型でなく `amadeus-developer-agent` で dispatch(`cid:reverse-engineering:c4` は read-only 作業の explore 型を既定とするが、ステージ本文 Step 2 が lead agent を名指ししている)。代償として `cid:practices-discovery:c2-engine-mutation-ban` / `cid:functional-design:c4-subagent-structural-guard` の禁止事項(engine 操作・git 状態変更・build・GitHub 書込・repo 内書込)をプロンプトへ明示し、書込先を repo 外 scratch 1ファイルに限定した
- 2026-08-11T14:10:00Z — **c5 引き取りの判定が誤りだった(訂正)**。`mismatch.json` 生成後にディスクが約20分不変だったことを根拠に `re-dev-scan` を停止と判断し、conductor が scratch 証拠から `developer-scan.md` を書いた。しかし subagent は停止しておらず成果物を composing 中で、直後に自版(541行、jq ベースの述語つきで conductor 版より網羅的 — `reason` 語彙分布・CI 配線 `.github/workflows/ci.yml:471-477`・`expiry` 保有 597件・distinct reason 310件・`<module>` スコープ 90件を追加)で上書きした。**教訓**: `cid:reverse-engineering:c3-disk-evidence-completion` のディスク実在述語は「目的成果物が単一の Write で最後に書かれる」形の subagent に対しては停止と composing を区別できない。中間生成物の更新停止を停止の証拠にしない。引き取り前に `idleReason: failed` の確認(`cid:units-generation:c3-session-limit-stall-diagnosis`)を待つべきだった。実害はなし(subagent 版が正本として残り、Architect にはそれが渡っている)
- 2026-08-11T13:38:30Z — 本セッションは worktree 隔離ガード下にあり、リダイレクト・ループを含む複合 Bash が拒否される(`cid:code-generation:c1-pcp-isolated-session-swarm-incompat` と同族の実測)。subagent 完了検知は複合ポーリングでなく Monitor の単純 until ループ + ディスク実在で行う(`cid:reverse-engineering:c3-disk-evidence-completion`)

## Tradeoffs
<!-- example: 2026-05-29T10:14:32Z — picked TDD over BDD this run; the team is unit-first and the domain is well-understood -->
- 2026-08-11T13:50:00Z — conductor 側の独立スポット照合(Developer scan と二重化)。測定 ref = worktree HEAD `854692fd7`。台帳は `tests/.coverage-patch-allowlist.json`、**623 エントリ / 106 ファイル**(`python3` で `json.load` 後に `len(sites)` と `len({s['file'] for s in sites})` を集計)、セレクタのキー形状は全 623 件が `('anchorLines','fingerprint','function','targetLines')` で単一(`cid:requirements-analysis:numbers-from-command-output-only`)
- 2026-08-11T13:51:00Z — Issue #1622 の中核主張を `tests/coverage-patch-gate.ts` の実読で確認。(a) `resolveSemanticSelector` `:293-310` は関数スコープ一致とフィンガープリント一致をそれぞれ「ちょうど1件」に要求し、外れれば throw する **fail-closed** — つまりセレクタは *drift* に対しては自己検証する。(b) しかし `findStaleAllowlistEntries` `:407-419` の逐語は `for (let line = e.start; line <= e.end; line += 1) { if (hits.has(line)) return false; }` であり、**解決後レンジが計測可能行を1本でも含めば非 stale** と判定するだけで、`reason` と行内容の意味一致を一切見ない(Issue の「存在検査のみ」主張は真)。(c) 帰結として、移行元の行ピンが誤っていたエントリは *誤った実在コード* に対してフィンガープリントが計算されており、解決は一意に成功し throw もせず stale 検査にも映らない — **転位が暗号学的に固定された**状態になる。フィンガープリントが与えるのは安定性であって正しさではない
- 2026-08-11T13:52:00Z — 意味整合の検査面は `reason` だけでなく `expiry`(`:322` `condition under which the entry should be removed`)にも及ぶ。623 件中どれだけが expiry を持ち、その解除条件が現行コードで既に成立していないかは Developer scan / 後続ステージの対象
- 2026-08-11T14:02:00Z — Architect の独立再検証が Developer scan の**不在主張の手続き誤り**を捕捉した(scan record §2「P2c の訂正」)。scan は5つの選択肢を `|` で連ねた1本の `grep -rniE` を実行して「0 hit(出力なし)」と記録したが、observed で再実行すると `ugrep: error: error at position 247 ... exceeds complexity limits` で **exit 1 の異常終了**であり、空出力は 0 hit ではなく**エラーの空出力**だった。述語を2本へ分割し `git grep` で再実測して 0 hit を確定(結論自体は不変)。教訓: `cid:requirements-analysis:absence-claim-grep-verify` は不在主張に反証確認を求めるが、**その反証確認に使う述語自体が異常終了しうる**面は未被覆 — 不在主張の根拠にする grep は exit code を確認し、複雑な選言述語は分割する
- 2026-08-11T13:56:00Z — P6(区間の患部交差)を conductor が実測。述語 = `git diff --name-only ce3c3ccfd 854692fd7 -- tests/coverage-patch-gate.ts tests/.coverage-patch-allowlist.json tests/unit/t229-coverage-patch-gate.test.ts tests/integration/t229-coverage-patch-gate-check.test.ts tests/README.md` → 出力は `tests/.coverage-patch-allowlist.json` **1件のみ**。すなわち**ゲート実装・その unit/integration テスト・README は区間内で無変更**であり、`coverage-patch-gate.ts` への行番号引用は base 断面の記録からそのまま current(`cid:reverse-engineering:upstream-cite-reresolve-on-shift` の再解決は当該ファイルには不要)。動いたのは台帳本体だけ
- 2026-08-11T13:57:00Z — 台帳の区間デルタ: `git diff --numstat` = `109 10`(109 追加 / 10 削除)。エントリ数は base `ce3c3ccfd` で **614 件 / 103 ファイル** → observed `854692fd7` で **623 件 / 106 ファイル**(いずれも `git show <ref>:tests/.coverage-patch-allowlist.json | python3 -c "json.load; len"` からの転記)。区間で純増 +9 エントリ / +3 ファイル

## Open questions
<!-- example: 2026-05-29T10:14:32Z — confirm the retention window with compliance before the next stage hardens the schema -->
- 2026-08-11T13:51:45Z — subagent の生死判定は**中間生成物の mtime でも体感経過時間でもなく、transcript の mtime と現在時刻の突合**で行える。実測: `ls -lt ~/.claude/projects/<project-slug>/<session-id>/subagents/` の `agent-<name>-<id>.jsonl` が `date` の現在時刻と一致 → 稼働中。本 intent で `re-architect` の生存をこの述語で確定した。あわせて先の誤引き取り(13:38 の Deviations 参照)の一次原因も判明 — 「約20分ディスク不変」という conductor の見積もり自体が誤りで、実際は数分だった(ターン進行が速く経過時間の体感がずれる)。`cid:requirements-analysis:numbers-from-command-output-only` は数値を「コマンド出力からの転記のみ」と定めるが、**経過時間もその対象**であり体感で書いてはならない。§13 の学習候補
