# Election Record — E-SRA-RA1

- question: intent 260805-semi-redefine-autonomy-f の requirements-analysis 明確化質問6問(Q1〜Q6)の一括裁定。正本は record の inception/requirements-analysis/requirements-analysis-questions.md(全文を実読すること)。conductor 提案は各問の agent recommendation をそのまま採用: Q1=B(semi 専用 authorization 型を新設し scope/effect/basisFingerprint の3責務だけ型で明示。grant の意味論は持ち込まない)/ Q2=A(set-mode に policies と方針込み確認 digest を載せ、--policies-file の非 full 無音破棄を loud 化)/ Q3=A(semi は全5段を使う。AUTONOMOUS_BLOCK_CAP と budget mode は不変、走行単位の主張は「質問で止まらない」に限定)/ Q4=A(guard hold の前に autonomy 認可 + engine provenance receipt の第2経路を置き、run_required: true の advisory は強制実行のまま維持)/ Q5=A(--autonomy は semi|full の2値、値を必ず consume、none と不正値は loud、再宣言は同値 no-op / 異値 loud。フラグ自体を provenance とみなさない)/ Q6=B(t431 の test を2分割し walking-skeleton ピンを独立 test として保存したうえで semi 質問封鎖ピンを反転)。各推奨の根拠と file:line は questions ファイル本文にある。既決事項(semi = full − 節目、後方互換なし、FR-GRT-004 維持、cap 不変、5段、advisory は plugin 非依存の一般形)は質問対象外であり、これらと矛盾する選択肢を選ばないこと。

裁定: 6問すべて推奨どおり採用(choice 1: 2票)
内訳: choice1=2票 choice2=0票 choice3=0票
- 留保(subagent-2, GoA2): 6問の推奨はいずれも既決境界と矛盾せず実装可能と確認したが、要件本文へ落とす際に2点の訂正を条件とする — (1) Q1 で選択肢 A の欠点として書かれた「modeProvenance は本来『誰が mode を設定したか』の記録であり、認可の器としては意味を拡張することになる」は実測と食い違う: modeProvenance は既に semi の認可述語そのものであり(amadeus-intent-autonomy.ts:512 `if (!internalGate || projection.modeProvenance.kind !== "human-command")`、:516 の `kind: "semi-mode-gate"` と :478 の型宣言)、裁定記録の principal 供給元でもある(:602-604 `input.projection.modeProvenance.kind === "human-command" ? input.projection.modeProvenance.principalId : "system-default"`)。B を採るのは構わないが、要件・設計では「A は provenance の意味を拡張する」ではなく「semi の認可基体は既に modeProvenance だが、scope/effect/basisFingerprint の3責務が型に現れていない」を B の採用理由として書き直すべき(cid:requirements-analysis:mechanism-cite-verify-at-draft / cid:application-design:citation-semantics-check)。(2) Q4-A の「介入は2ファイルに閉じる」は receipt の永続面を過小評価している: AdvisoryChoiceReceipt は humanTurn を必須に持ち、重複排除が humanTurn.eventIdentity をキーにし(amadeus-advisory-choice.ts:875-878)、提示照合も humanTurn 引数を取る(:889 `hasMatchingAdvisoryPresentation(projectDir, open, humanTurn)`)。第2経路を置くには receipt schema(:837 近傍の `schema: 1` 書込み)を provenance の判別ユニオンへ**置換**する必要があり、humanTurn 版と engine 版を並存二重実装として書くと org.md Forbidden(要求されない二重実装)に抵触する。要件へは「receipt の provenance を判別ユニオンへ置換し、旧形の並存を作らない」と明記すること。あわせて、run_required 経路は plugin 非依存ではない(formalCheckRoute:685 が `"plugins/formal-model-check/tools/run-model-check.ts"` と `specs/tla/FormalElection.tla` をハードコード)ため、In-7 の「plugin 非依存の一般形」は guard の hold 判定と無人裁定経路に射程を限定して書くこと。以上はいずれも推奨の可否を変えない。
- 留保(subagent-1, GoA2): 6問の推奨はいずれも採用でよいが、requirements.md 起草時に次の3点を明記しないと無申告逸脱・fail-open になる: (1) Q3=A の『全5段』は承認済み上流(scope-document.md:11 In-1 と intent-statement.md:20 がともに逐語『無人解決4段(方針なしは3段縮退)』)と数値が食い違うため、RE の実測訂正(inception/reverse-engineering/memory.md:8『無人解決の段数は #2253 が主張する「4段」でなく 5段(confirmed-policy を数え落としている)』、実装は amadeus-intent-autonomy.ts:706-744 の confirmed-policy/norm/history/solo-election/agent-recommendation)を根拠とする訂正申告段落を要件に置く(cid:requirements-analysis:approval-lineage-citation / cid:requirements-analysis:implementation-deviation-election)。(2) Q4=A の第2 receipt 経路は fail-closed であることを受け入れ基準で固定する — recordProtectedAdvisoryChoice(amadeus-advisory-choice.ts:864-868)が humanTurn 必須+isGroundedHumanTurn(:852-861)で監査 HUMAN_TURN と timestamp/digest 一致を照合している現行保証を、autonomy 認可が成立した場合に限り AUTO_DECIDED の basisFingerprint で代替する形にし、認可不成立時に第2経路へ落ちない(=人間経路の緩和にならない)ことを『落ちる実証』込みで要求する(org.md Forbidden の検証劇場対策)。(3) In-1 が言う『amadeus-stop.ts の質問 carve-out 述語の grant 非依存化』の対象述語 isFullyAutonomousIntent(:167-176)は質問 carve-out(:422 isPendingQuestionStop)だけでなく compose stop(:457 isPendingComposeStop)と conversational stop(:716)からも共有されているため、要件は変更する呼び出し点を列挙して射程を確定する(無条件の述語書き換えは Q3=A が『不変』と主張する範囲外の挙動まで semi へ開く)。
票タイムライン: subagent-2 2026-08-05T08:20:00Z(受理 2026-08-05T06:18:43Z) → subagent-1 2026-08-05T08:05:00Z(受理 2026-08-05T06:18:58Z) → 配信 2026-08-05T06:24:04Z → 配信 2026-08-05T06:24:04Z → 開票 2026-08-05T06:24:04Z
GoA[E-SRA-RA1]: 1x0 2x2 3x0 4x0 5x0 6x0 7x0 8x0
