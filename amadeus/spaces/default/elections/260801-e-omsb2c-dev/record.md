# Election Record — E-OMSB2C-DEV

- question: Bolt 2c(U5 metrics)builder 申告の契約逸脱・設計判断6件の裁定(実装済み・全検証 green。worktree = .amadeus/worktrees/bolt-metrics、報告全文 = scratchpad/b2-metrics-report.md)。(1) registerMeterProvider へ projectDir 追加 — registeredMeterProjectDir() 実装に必須で tracer/logger と対称の一意導出、呼出し元3箇所更新済み。(2) metrics-vocabulary.ts の leaf 分離 — redaction が metric 次元を admit する必要があり1ファイル案は import 循環になる実測。(3) supplyTokenUsage は setTokenUsageSink 注入 — resource-suppliers の LEAF 不変条件と循環回避(既存 generation counter と同じ反転パターン)。(4) gate.iterations の発火点 = stage.revising — GATE_REJECTED と対 emit のため両方数えると二重計上。(5) subagent.duration は SUBAGENT_STARTED 登録(U4 別 Bolt)待ち — observer 配線済み・in-process 検証のみ。(6) 跨プロセス duration にマーカーファイル(telemetry dir の pending-<kind>-<key>.start)新設 — stage の started/completed が別プロセスのため in-process pairing では production 計測0件。Relay store 選別に非一致をテスト固定済み。

裁定: 6件全て承認(実装受理)(choice 1: 2票)
内訳: choice1=2票 choice2=0票
- 留保(subagent-1, GoA2): (6) のマーカー機構は受理するが、stale marker の回収経路が実装にもテストにも不在(takeDurationStart の消費だけが削除点で、未完了ステージのマーカーは同一 key の再 start による上書きまで残る)。key 空間が stage slug / agent type に限られるため増加は有界で fail-open のため実害は測定漏れに留まるが、Issue 起票の上で追跡すること。あわせて t399:215 の Relay 非一致テストは readdir 結果に .jsonl が無いことの確認であり、Relay の selector そのものを駆動していない — selector 変更時に無音で失効しうるので、STORE_PREFIX を実際に適用する形へ強化することを推奨する。いずれも本 Bolt の差し戻し理由には当たらない。
- 留保(subagent-2, GoA2): (a) (2)(3) の循環主張は結論は正しいが機序の記述が不正確 — 報告が挙げる連鎖 `metrics-instruments → meter-provider → local-metric-exporter → redaction` のうち meter-provider:26 は `import type { LocalMetricExporter }` の型専用辺であり、bun/tsc で消去されるため runtime 循環は閉じない。(3) の `resource-suppliers → ... → resource.ts` 側も metrics-instruments:38 が `import type { TokenUsage }` の型専用辺で同様。したがって『循環になる実測』は成立していない。ただし決定自体は独立に正しい: (2) は redaction.ts が既に leaf の event-registry.ts(import 0行)から REGISTERED_EVENTS を引く既存イディオムと同型で、metrics-vocabulary.ts も import 0行の leaf として実測一致する。catalogue を instruments 側に置けば redaction が amadeus-lib.ts(loadStageGraph)と node:fs へ依存を得る層違反が実在する。(3) は resource-suppliers.ts の LEAF 不変条件がコード上に明文で存在し反転注入がそれを保存する。以後の成果物・裁定文では『循環』でなく『型専用辺のため runtime 循環は閉じないが、leaf/層境界の保存のため分離した』と機序を訂正して記録すること(cid:requirements-analysis:mechanism-cite-verify-at-draft / cid:application-design:citation-semantics-check)。(b) (6) マーカー機構にキー衝突の未対処面がある — キーは stage slug / agent type のみで、markDurationStart は同一キーを無条件上書き、takeDurationStart は消費時に削除する(metrics-instruments.ts:113-140)。同一 agentType の subagent を並行 dispatch する運用(本 intent 自体が swarm・並行 Bolt を既定とする)では、後発の start が先発を上書きし、最初の completion のみが誤った短い duration を計測し、以後の completion は無計測になる。t399 に並行同一キーのテストは存在しない(`grep -n 'concurren|parallel|twice'` → 該当なし)。あわせて start に対応する completion が来ない場合(park・再 jump・中断)マーカーは永続残留し、除去経路は repo 全域に存在しない(`grep -rn 'pending-' packages/framework/core --include=*.ts` → metrics-instruments.ts:110 のみ)。いずれも fail-open で制御フローには波及せず、キー種別数で有界なため受理を妨げないが、Issue 起票のうえ本 intent 内で衝突耐性(キーへの一意識別子付与)と孤児回収を扱うこと。(c) (5) は現状 production 発火 0 件の dead wiring であり、これは U9 が指摘を受けた形と同型である — `grep -n 'subagent.started' packages/framework/core/otel/event-registry.ts` は 0 hit(subagent.completed は :477 に実在)で、registry 未登録のため canonical emit されない。U4 の登録着地までは observer の当該分岐が到達不能であることを認めたうえで、U4 着地後に in-process ではなく実 emit 経路での発火を実測確認してからでなければ本 intent を完了扱いにしないこと(cid:code-generation:ruling-premise-closure-verification)。
票タイムライン: 配信 2026-08-01T08:52:34Z → 配信 2026-08-01T08:52:34Z → subagent-1 2026-08-01T08:54:43Z(受理 2026-08-01T08:54:50Z) → subagent-2 2026-08-01T08:55:04Z(受理 2026-08-01T08:55:55Z) → 開票 2026-08-01T08:58:35Z
GoA[E-OMSB2C-DEV]: 1x0 2x2 3x0 4x0 5x0 6x0 7x0 8x0
