{"schemaVersion":2,"kind":"original","electionId":"E-260818-PBB4-FIX-METHODS","voter":"subagent-2","voterKind":"subagent","responses":[{"questionId":"q1-2837-context-contract","choiceInternalNo":3,"goa":2,"reservation":"C を採る根拠: batch identity は engine が権威的に算出しながら emit 境界で捨てている値(orchestrate.ts:3929 -> :4046 -> :4294 で emitConfiguredSwarm(:4074) が units のみ受領)なので搬送が正しい是正だが、check_cmd/test_file は搬送では解けない — amadeus-swarm.ts:231-234 が逐語で 'checkCmd is the user's own project check command (a trusted input)' と宣言し、git grep -n \"check_cmd\" -- packages/ は exit 1(エラーでない不一致)で権威値がフレームワークに存在しない。A のように engine が供給しない値を directive に載せると、どこも populate しないフィールド(検証劇場クラス)になる。B は directive を最小に保つ代償に新 CLI verb(amadeus-swarm.ts:1419 の Valid 一覧に context/status なし)と 7 面ぶんの呼出順 prose を増やし、しかも check_cmd の宣言先問題は解けないまま。以下を実装契約とする。(1) 搬送する identity は conductor がそのまま prepare --batch へ渡せる値であること。現行 prepare は ^[1-9][0-9]*$ のみ受理し(amadeus-swarm.ts の handlePrepare 冒頭)その値がそのまま durable な pool identity になる(:638 idempotencyKey `unit-pool:${flags.batch}:initial-enqueue` / batchId)。pool generation を織り込んだ結果が非数値になるなら prepare と pool 側の受理形も同一変更で決め、engine が prepare に拒否される値を emit しないこと。(2) identity が DAG index から乖離するなら、index を鍵にする既存 join を grep で全列挙して同一変更で整合させる: firstUncoveredBatch の readProjection(String(index+1)) (:3918-3922)、owedBatchGate / approve-batch 台帳の 1-origin、readPerUnitConsumePopulation の foldUnitPoolEventSets(eventSets, String(index+1)) (:2527) と settle 行 batch 突合 (:2549)、settlePerUnitOutcomes の batchOf (:4700)。一部だけ直す合意述語ドリフト(cid:code-generation:cg2-agreeing-predicate-drift)を避ける。(3) prepared_batch / retry_unit arm(:4092-4106、validator :620-631)と同一概念に別名を作らない。INVOKE_SWARM_FIELDS(amadeus-directive.ts:555)と FIELD_CHECKS(:661)へ新フィールドと retry arm との排他/含意関係を明示し validator テストを足す。t135(--batch がハードコード)/ t113 / t181 conductor skill parity を連動更新する。(4) FR-2837-2 は『check_cmd / test_file は conductor(ユーザー)知識であり engine は供給しない』を契約文書へ明記し、7 conductor 面(claude/codex/kimi/kiro/kiro-ide/cursor/opencode)に正規取得元を書いて per-surface grep が各面 1 hit 以上(exit 0)になることで閉じる。本 intent で config キー等の宣言先を新設しない — 必要なら別 Issue(requirements の Out of Scope 規律)。(5) amadeus-directive.ts:306-311 の 'the conductor reads the rest of the batch context off the compiled runtime graph' は batch について偽になるので同一変更で訂正する。(6) FR-2837-4 の回帰テストは failed terminal -> upstream replan -> 同一 Unit 再提示(uncovered 判定 :3923-3928 は cancelled/succeeded のみ除外し failed を除外しない)を通し、emit された directive が運ぶ identity が旧 terminal pool と衝突しないことを directive 実物に対して assert する(--batch のハードコードで代替しない)。","rationale":null},{"questionId":"q2-3106-outcome-recording","choiceInternalNo":1,"goa":2,"reservation":"A を採る根拠: reviewer-2 の seam 実験 B/C が『cancelled を明示的に1行記録すれば fanout はそのまま通り当該 unit の path も正しく除外される』を実測し、KNOWN_OUTCOMES(per-unit-consume-fanout.ts:199)は既に cancelled/failed を受理するため読み口変更は不要で修正が emitter 側に閉じる。C は E-260815-3099-C-FORM の established(C2 = 別イベント新設、pool の単一 writer 契約は不変)と FIX-METHOD の『pool 捏造禁止』制約、および同 record の subagent-2 留保『solo:<n>:<unit> 形式の非数値 id を持ち込むと batch join が退化する』を正面から覆すため、再裁定なしには採れない(C1 は同選挙で 0 票)。B は同じ FIX-METHOD で choice A(読み口統一)として 0 票、かつ canonical projection が {intent, stage} スコープ(projectConstructionOutcomes)であるため consumer stage から producer stage を解決する新次元が要り、solo batch id が非数値で C-FORM の共通制約『数値 batch join を逐語保存』に抵触し、outcome 供給源が3系統になって producer-outcome-ambiguous 誘発リスクを持つ。以下を実装契約とする。(1) 閉語彙は {succeeded, cancelled, failed} のちょうど3値。readSettledUnitOutcomes(:2499-2511)は語彙外・鍵欠落を INVALID_SETTLED_ROW で throw し続け、:2471-2475 の『編集された台帳が consumer の実行可否を決められない』保証を弱めない。SettledUnitOutcome.outcome の型も union へ更新する。(2) cancelled / failed の行は unitCovered ゲートに載せない — :4707 の coverage 述語は succeeded arm 専用のまま残す(reviewer-1 は成果物ディレクトリ削除下でも発火を実測)。cancelled/failed の値は canonical construction outcome projection(cancelledConstructionUnits :3934-3946 と同一源)から導出する engine 観測事実に限り、conductor 供給値を受け取らない。(3) E-260815-3099-C-FORM の共通制約を逐語保存する: 数値 batch join(:2527 / :2549)と pool 優先 de-dup(:2546-2551 — pool terminal を持つ unit には settle 由来 outcome を積まない)。同一 unit が 2 行にならないことをテストで固定する。(4) 冪等鍵 perUnitOutcomeKey(:2485 = stage+unit+batch)のままでは cancelled 行が1度付いた後に unit が再実行・再成功しても上書きできず、台帳が事実に反したまま consumer の可否を決める。supersession 規則(鍵に outcome を含めたうえで reader が audit sequence 最大の行を採る等)を設計で明示決定し、shard ファイル順に依存しない決定的順序で固定して、cancel -> BOLT_STARTED 再入(construction-outcome-projection.ts:262-265 の currentTerminals delete)-> success の系列をテストで固定する(write<->read の round-trip 観点)。(5) 落ちる実証は t533 integration :786-801(pool 版 cancelled)の対に per-unit settle x cancelled の Red を先に置き、加えて failed 版(producer-outcome-failed を assert)も修正前に置く — RA iteration-2 NIT のとおりエラーパスのテスト後回しはしない。(6) 設計成果物に E-260815-3099 系裁定との関係を明記する: `Outcome: succeeded` 限定は #3105 の実装判断(:2475)であり、FIX-METHOD の subagent-1 留保が『outcome=succeeded の根拠が足りないと実装時に判断される場合は、その場で緩めず再裁定へ戻すこと』と定めていた — 本選挙がその再裁定であると書き、既決裁定の無申告の上書きにしない。あわせて FR-3106-4(docs/guide/15-troubleshooting.md 英日)と台帳 resync(model-map 実装ハッシュピン / coverage-patch-allowlist 意味的セレクタ / 新規テストファイル時の coverage-registry regen)を同一変更に含める。","rationale":null}],"submittedAt":"2026-08-18T08:15:29Z","receivedAt":"2026-08-18T08:18:00Z"}