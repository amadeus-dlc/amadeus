# フロントエンドコンポーネント — unit: parallel-policy-docs

## 適用判断

本成果物は CONDITIONAL（unit が frontend / UI を含む場合だけ作成する）である。

本 unit は memory policy と phase memory の文書変更だけを扱い（requirements.md 制約: 変更は文書のみ）、frontend / UI コンポーネントを含まない。

## 判断根拠

- requirements.md の R001〜R006 に UI に関する要求がない。
- 変更対象（team.md、construction.md、Intent record 配下）はすべて Markdown 文書である。

したがって、本 unit にフロントエンドコンポーネント設計は不適用である。
