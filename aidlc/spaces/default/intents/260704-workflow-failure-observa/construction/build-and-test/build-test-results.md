# Build Test Results

## 入力成果物

この結果は、各 Unit の `code-generation-plan` と `code-summary` を入力として実行した。

対象は U001、U002、U003 の 3 Unit である。

## Command Results

| Command | Exit | Result |
|---|---:|---|
| `npm run typecheck` | 0 | success |
| `npm run lint:check` | 0 | success |
| `npm run contracts:check` | 0 | success |
| `npm run claude-wiring:check` | 0 | success |
| `npm run diff:check` | 0 | success |
| `npm run test:it:failure-evidence-foundation` | 0 | success |
| `npm run test:it:subagent-status-audit` | 0 | success |
| `npm run test:it:workflow-warning-traceability` | 0 | success |
| `npm run test:it:all` | 0 | success |
| `npm run test:it:engine-e2e` | 0 | success |
| `bun audit` | 0 | success, no vulnerabilities found |
| `AmadeusValidator.ts . 260704-workflow-failure-observa` | 0 | pass |
| `npm run parity:check` | 1 | failed, 8 engine file hash mismatches |
| `npm run test:all` | 1 | failed at `parity:check` |

## Failure Details

`npm run parity:check` failed with 8 engine file hash mismatches.

Affected paths are:

- `aidlc-common/stages/inception/practices-discovery.md`
- `hooks/aidlc-log-subagent.ts`
- `knowledge/aidlc-shared/audit-format.md`
- `tools/aidlc-lib.ts`
- `tools/aidlc-orchestrate.ts`
- `tools/aidlc-state.ts`
- `tools/aidlc-utility.ts`
- `tools/data/stage-graph.json`

`npm run test:all` failed because it runs `parity:check` before later checks.

## Pass Counts

Additional Unit eval commands passed 3 of 3.

Integration eval commands passed 2 of 2.

Build and static checks passed 5 of 5.

Security dependency scan passed 1 of 1.

Parity checks passed 0 of 1 for the live workspace check.

## Coverage Report

No line coverage report is generated by the existing eval suite.

Requirement coverage is evidence-based.

R001 through R009 each have at least one deterministic evidence item in U001、U002、or U003 evals.

## Resolution Path

The parity failure is not hidden.

Next action is to decide whether the changed engine and hook files require a distribution baseline update, an upstream contribution path, or a human-approved exception.

`dev-scripts/data/parity-map.json` remains unchanged.
