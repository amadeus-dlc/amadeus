---
name: grilling-integration
depth: Standard
keywords: []
---

# Scope: grilling-integration

Composed scope for integrating mattpocock's grilling skill as "Amadeus Grilling"
(a fourth stage-protocol interaction mode plus a standalone read-only session
skill) into this brownfield framework repo. 11/32 stages EXECUTE: the three
initialization stages, intent-capture, scope-definition, reverse-engineering,
requirements-analysis, user-stories, functional-design, code-generation, and
build-and-test. Everything else SKIPs: the repo has no deploy target
(distribution = dist copy; "deployment" is promote:self inside build-and-test),
no GUI (no mockups), a single team (no team-formation), an already-populated
practices layer (no practices-discovery), and component boundaries fully
predetermined by the existing architecture (no application-design /
units-generation — functional-design designs directly against the
reverse-engineered codekb, mirroring the stock refactor scope's accepted
shape; the functional-design/code-generation upstream advisories were accepted
at the compose gate).

Not inferable by keyword — reachable only via `--scope grilling-integration`
(keywords granted: none).
