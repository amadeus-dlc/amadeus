// Fixture: the canonical (distribution-truth) side of the copy-residue scenario
// for predicate 2. Shares its basename with the scripts/ sibling so the
// intersection is non-empty (red — copy residue).
export const FIXTURE = "duplicate-scenario:canonical";
