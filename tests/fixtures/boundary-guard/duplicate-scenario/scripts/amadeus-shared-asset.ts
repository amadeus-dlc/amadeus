// Fixture: the `scripts/` side of the copy-residue (both-present) scenario for
// predicate 2 (findDuplicatedAssets). Its basename intentionally collides with
// the canonical/ sibling so the integration twin can prove the red state.
export const FIXTURE = "duplicate-scenario:scripts";
