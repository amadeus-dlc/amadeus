# Test Pro Skill

A prose artifact the projector transforms per harness. It references
{{HARNESS_DIR}} and {{HARNESS_DIR}}/rules/test-pro so the token substitution and
the rules-dir rename are observable in the projected output for every face.
